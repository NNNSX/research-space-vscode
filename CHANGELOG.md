# Changelog

All notable changes to **Research Space** are documented here.

## [2.3.2-alpha.1] — 2026-04-28

- **宠物陪伴系统升级** — 宠物支持更完整的画布感知、全画布跟随、低打扰建议、长期记忆摘要、可解释建议卡片和成长展示。
- **文件转换依赖完整性修复** — 恢复通用 VSIX 打包策略，并补充运行时依赖检查，确保 PDF 转 PNG 所需的 PDF worker 与 canvas runtime 随包携带。
- **宠物交互修复** — 修复跟随模式拖动偏移、建议卡片靠近屏幕边缘时越界等问题。
- **发布包元数据修正** — 修正扩展包仓库信息，确保指向正确的 GitHub 仓库。

## [2.3.1-alpha.11] — 2026-04-28

- **建议卡片多动作候选** — Pet Brain 建议卡片支持多个候选动作，例如结构导图 / 材料笔记、整理笔记 / 结论导图。
- **操作边界说明** — 建议卡片为每个动作补充原因、风险等级和操作边界说明，明确所有动作都只在用户确认后执行。

## [2.3.1-alpha.10] — 2026-04-28

- **Pet Brain 建议卡片 MVP** — 宠物上下文建议从纯气泡升级为“气泡 + 建议卡片”，卡片会说明建议原因。
- **确认后轻操作** — 针对结构整理、输出收口和错误恢复建议，用户确认后可新建导图、新建整理笔记或打开 AI 设置；所有操作都需要确认。

## [2.3.1-alpha.9] — 2026-04-28

- **宠物建议采纳统计** — 上下文建议气泡新增“采纳”入口，宠物会记录建议展示、采纳、稍后和屏蔽次数，并计算采纳率。
- **Phase 3 记忆闭环收口** — “我学到了”区域同步展示建议采纳情况，为后续建议卡片和 Pet Brain 提供偏好依据。

## [2.3.1-alpha.8] — 2026-04-28

- **宠物偏好学习增强** — 长期记忆会从画布状态中学习常用节点类型、常用 AI 工具和常见写作场景，并在“我学到了”区域展示。
- **建议响应统计** — 宠物会记录上下文建议的展示、稍后和屏蔽次数，用于后续降低打扰和调整建议风格。

## [2.3.1-alpha.7] — 2026-04-28

- **我学到了面板** — 宠物设置中的长期记忆区域新增轻量摘要，展示主动建议偏好、显示模式、近期事件类型和最近会话记忆。
- **记忆过期清理** — `pet/memory.jsonl` 支持 `expiresAt` 过期字段，读取摘要时会自动跳过并清理过期记录。

## [2.3.1-alpha.6] — 2026-04-28

- **宠物长期记忆 MVP** — 宠物记忆拆分为 `pet/state.json`、`pet/profile.json` 和 `pet/memory.jsonl`，长期记忆只保存轻量使用摘要和事件类型，不保存源文件正文。
- **记忆可控** — 宠物设置新增本地长期记忆开关和清空入口，可删除 profile、jsonl 和旧版 memory.md，同时保留宠物等级与位置状态。

## [2.3.1-alpha.5] — 2026-04-28

- **全画布宠物手动放置** — 全画布跟随模式下，宠物可被拖到画布可视区域内的任意位置，并会随宠物状态保存。
- **恢复自动跟随** — 手动放置后可通过双击宠物或点击重置按钮恢复自动跟随选中节点 / 视口角落。

## [2.3.1-alpha.4] — 2026-04-28

- **宠物全画布跟随模式** — 宠物设置新增固定小面板 / 全画布跟随两种显示模式；全画布跟随会把宠物作为画布内轻量悬浮角色，跟随当前选中节点或停靠视口角落。
- **跟随模式性能保护** — 画布缩放较小时宠物自动进入轻量态，保持屏幕尺寸稳定，避免随画布缩小到不可点击。

## [2.3.1-alpha.3] — 2026-04-28

- **宠物主动建议可控** — 宠物设置新增勿扰、安静、平衡、活跃四档主动建议，用户可控制画布事件气泡的打扰程度。
- **宠物建议气泡交互** — 上下文建议气泡新增“稍后”和“不再提醒”轻交互，降低重复提醒对画布编辑的干扰。

## [2.3.1-alpha.2] — 2026-04-28

- **宠物画布感知增强** — 宠物聊天上下文加入画板、节点组、蓝图、思维导图、工具节点、最近 AI 输出、场景判断、阶段判断和可能的整理阻塞信号。
- **宠物事件提醒优化** — 画布节点新增 / 删除、连接、选中、AI 完成和 AI 失败事件统一为事件流，并加入重要度与冷却策略，减少重复打扰。
- **本地规则建议** — 当短时间新增多份材料、连续生成多个 AI 输出或工具多次失败时，宠物会低频提示导图整理、输出收口或错误恢复方向。

## [2.3.1-alpha.1] — 2026-04-28

- **思维导图节点阶段收口** — 轻量思维导图节点支持大纲编辑、脑图式图形编辑、图片引用、Markdown / XMind 导入导出，并在画布节点上以 SVG 缩略图展示一级结构。
- **导图封面体验优化** — 导图封面只展示中心主题与一级分支，超过 4 条一级分支时显示“+N 更多”，避免封面拥挤或误以为后续分支未保存。
- **图形编辑稳定性修复** — 修复导图曲线连接、图片分支排版、重复条目 ID、保存重开后分支连线丢失、以及封面漏显示一级分支等问题。
- **画布编辑浮层修复** — 修复选中节点后的悬浮工具条遮挡画板编辑、导图编辑等编辑层的问题。

## [2.2.2-alpha.48] — 2026-04-28

- **导图封面分支省略提示** — 思维导图封面仍最多渲染 4 条一级分支；当一级分支超过 4 条时，缩略图右上角会显示“+N 更多”，避免误以为后续分支没有保存或没有更新。

## [2.2.2-alpha.47] — 2026-04-28

- **修复导图封面漏显示一级分支** — 思维导图摘要新增独立的一级分支标题列表，画布封面不再依赖 DFS 文本预览，避免子分支占用摘要名额后漏掉“支撑材料”等同级分支。

## [2.2.2-alpha.46] — 2026-04-28

- **导图封面只展示一级结构** — 画布上的思维导图 SVG 缩略图只渲染中心主题和一级分支，不再显示二级子分支，避免节点封面过挤。
- **修复画布悬浮工具条遮挡编辑层** — 降低选中节点工具条层级，并提高画板编辑与导图编辑弹窗层级，避免“移动 / 导出”等悬浮按钮盖住编辑框内容。

## [2.2.2-alpha.45] — 2026-04-28

- **修复导图缩略图层级显示** — 画布上的导图 SVG 缩略图现在会按大纲缩进解析父子关系，只展示前两级但保持层级语义正确，避免子分支被误画成一级分支。
- **修复画板编辑弹窗崩溃** — 修复点击画板右上角编辑按钮时 `ReactDOM is not defined` 导致的 Webview Render Error。

## [2.2.2-alpha.44] — 2026-04-28

- **导图节点缩略图展示** — 画布上的思维导图节点从纯文本摘要改为轻量 SVG 导图缩略图，直接显示中心主题、分支曲线和分支标题，用户无需打开编辑器也能快速识别导图结构。
- **导图卡片信息收敛** — 分支数、条目数和图片数改为缩略图底部轻量标注，减少卡片内文本列表占用空间。

## [2.2.2-alpha.43] — 2026-04-27

- **修复导图重开后部分分支无连线** — 导图读取和保存时会自动修复重复的条目 ID，避免多个同名分支在重开后共用同一个节点引用，导致统一 SVG 连线层只能连接到其中一个分支。
- **导图数据容错增强** — 图片引用 ID 也会在加载时去重，降低旧数据、导入数据或异常保存数据对图形渲染和后续编辑的影响。

## [2.2.2-alpha.42] — 2026-04-27

- **脑图连线层重做** — 思维导图图形编辑模式改为整张导图统一 SVG 连线层，按真实条目位置绘制中心主题、一级分支和子分支之间的贝塞尔曲线，避免图片尺寸或分支高度变化后连接线被布局挤歪。
- **图文分支观感优化** — 移除分支列上的辅助边框，文本胶囊、图片卡片和选中态分层更清晰，降低图片与标题混在一起造成的视觉拥挤。

## [2.2.2-alpha.41] — 2026-04-27

- **脑图连线视觉修正** — 移除上一版用边框拼接导致的异常大弧线和竖向括线，改为轻量 SVG 贝塞尔连接，减少大图片分支附近的错位与包围感。
- **图片分支排版优化** — 带图片的导图条目改为“标题胶囊 + 图片卡片”的分层结构，选中态不再把文本和大图一起包成巨大椭圆，图片边框、阴影和间距更稳定。

## [2.2.2-alpha.40] — 2026-04-27

- **脑图连线曲线化** — 思维导图图形编辑模式的主干与分支连接从正交直线调整为更接近常规脑图的圆角曲线风格。
- **图片引用可调尺寸** — 选中带图片的导图条目后，可在图形编辑侧栏调整图片宽高，图片尺寸会保存到导图数据中。
- **大纲同步图片信息** — 大纲模式现在会显示图片引用行，并保留图片所在条目位置和尺寸标记，避免图形模式添加图片后切到大纲看不到对应信息。

## [2.2.2-alpha.39] — 2026-04-27

- **思维导图自动化验证补齐** — 新增 VS Code Custom Editor 集成测试，覆盖思维导图文件保存、画布节点摘要同步、关闭后重开画布以及导图结构重载，减少只能依赖人工烟测的问题。
- **保存链路复用收口** — 将导图保存与画布节点摘要同步抽为共享链路，Webview 消息处理和自动化测试使用同一套逻辑，降低保存 / 重载行为不一致风险。

## [2.2.2-alpha.38] — 2026-04-27

- **思维导图体验收口** — 图形编辑模式在没有分支时显示明确空状态，可一键添加第一个分支；侧栏操作提示补充拖拽平移和滚轮缩放说明。
- **保存状态更清晰** — 导图保存时显示“保存中 / 已保存 / 保存失败”状态；保存失败时会提示当前弹窗编辑内容仍保留，避免用户误以为磁盘文件已被覆盖。
- **导图节点卡片优化** — 画布中的思维导图节点新增结构化摘要卡片，显示一级分支、总条目、图片数量和分支预览，减少 resize 或缩放时内容溢出。

## [2.2.2-alpha.37] — 2026-04-27

- **XMind 导入** — `.xmind` 文件可通过资源管理器“Add to Canvas”或直接拖入画布导入，并自动转换为 Research Space 思维导图节点，画布仍保存 `.rs-mindmap.json` 结构文件。
- **XMind 导出** — 思维导图编辑器新增“导出 XMind”，可通过 VS Code 保存对话框导出 `.xmind` 文件，便于与外部脑图工具交换基础结构。

## [2.2.2-alpha.36] — 2026-04-27

- **思维导图 Markdown 导出** — 导图编辑器新增“导出 Markdown”，通过 VS Code 保存对话框选择路径，不静默覆盖文件；图片引用会按 Markdown 图片语法保留，缺失图片会在导出内容中提示。
- **选中节点导出支持导图** — 画布“导出 MD”选中思维导图节点时，会读取 `.rs-mindmap.json` 并导出为 Markdown 大纲，而不是只导出节点预览。
- **脑图视图支持拖拽和缩放** — 图形编辑区支持鼠标拖动画布、滚轮缩放，并提供缩放按钮和“居中”按钮，避免只能靠滚动条移动视图。

## [2.2.2-alpha.35] — 2026-04-27

- **脑图视觉继续增强** — 思维导图图形编辑模式新增一级分支配色、左右主干线和分支连接线，分支层级比上一版更接近常见脑图视图。
- **保持轻量交互** — 继续保留点击编辑、添加同级 / 子级、折叠展开和图片缩略图，不引入复杂自由拖拽，避免提高学习成本。

## [2.2.2-alpha.34] — 2026-04-27

- **重做导图图形视图** — 思维导图“图形编辑”从纵向树状卡片改为中心主题 + 左右分支的脑图式布局，更接近 XMind 这类思维导图的基础视觉。
- **保留轻量编辑能力** — 新布局下继续支持点击编辑、添加同级 / 子级、折叠展开和图片缩略图，不引入复杂自由拖拽或主题系统。

## [2.2.2-alpha.33] — 2026-04-27

- **思维导图图形编辑** — 导图编辑器新增图形编辑模式，可直接点击条目修改文本，添加同级 / 子级分支，并支持折叠展开。
- **导图图片引用** — 可在导图条目中选择本地图片，导图文件只保存相对路径，不复制图片二进制；图形模式中会显示缩略图并可点击打开原图。
- **删除保护** — 删除含子级的导图分支前会确认，避免误删用户已整理的结构。

## [2.2.2-alpha.32] — 2026-04-27

- **新增思维导图节点 MVP** — 画布顶部工具栏可新建思维导图节点，放置后会保存为独立 `.rs-mindmap.json` 文件，画布只保留路径和摘要。
- **大纲编辑模式** — 双击或点击节点“编辑”可打开思维导图编辑器，用 `# 中心主题` 和缩进列表编辑多级分支，保存后节点标题和预览会同步更新。
- **导图内容可被 AI 工具读取** — 思维导图节点连接到文本类 AI 工具时，会按 Markdown 大纲提供内容，便于继续摘要、整理或转写。

## [2.2.2-alpha.31] — 2026-04-27

- **引用覆盖悬浮详情** — AI 输出节点底部的引用状态现在可悬浮查看详情，显示已引用来源、未引用来源、未连接标签和引用提醒。
- **引用展示逻辑模块化** — 引用覆盖 badge 的展示文本与详情提示独立成可测试逻辑，减少后续 UI 调整时破坏来源追溯信息的风险。

## [2.2.2-alpha.30] — 2026-04-27

- **文内引用覆盖度追踪** — AI 文本输出会记录每个连接来源是否在正文中被 `[资料1]`、`[资料2]` 等标签引用，并提示缺失或多出的来源标签。
- **引用状态可见化** — AI 输出节点底部会显示来源引用覆盖度；选中节点导出 Markdown 时也会带出引用覆盖、缺失来源和引用提醒，便于后续写论文、项目书和专利时追溯依据。
- **提示词引用要求加强** — 文本工具的系统提示进一步强调多来源综合引用、不要伪造来源、不要只在末尾列来源。

## [2.2.2-alpha.29] — 2026-04-27

- **公开变更记录收口** — CHANGELOG 进一步聚焦用户可见的新功能、修复和使用注意事项，减少与插件使用无关的实现过程描述。
- **文内引用说明更清晰** — 公开文档继续使用用户可理解的 `[资料1]` 来源引用表述，避免把开发过程细节混入面向用户的版本说明。

## [2.2.2-alpha.28] — 2026-04-27

- **文内引用规则覆盖到自定义工具生成** — 新建或导入的 LLM 文本工具会继续兼容 `[资料1]`、`[资料2]` 正文内引用机制，减少来源信息只堆在输出末尾的情况。
- **更新 AI 编排提示词** — AI 工具面板中的“AI 编排提示词”已加入来源引用规则，生成自定义工具 JSON 时会提示 systemPromptTemplate 不得破坏正文内引用机制。
- **内置文本工具提示词同步** — 摘要、文献综述、评审、翻译、RAG、自由对话、大纲和行动项等文本工具默认提示词已补充文内引用要求；自定义系统 Prompt 编辑区也增加运行时引用规则提示。
- **引用缺失提示** — 如果有连接输入但输出正文没有检测到 `[资料1]` 这类标签，输出节点 meta 会记录提醒，功能节点完成状态也会短暂提示用户检查结果。

## [2.2.2-alpha.27] — 2026-04-27

- **AI 输出改为正文内引用** — 普通文本 AI 工具不再在输出末尾追加 `## 依据说明`，避免把来源信息堆在正文之后。
- **提示词注入来源引用要求** — 运行文本工具时，会把连接输入标记为 `[资料1]`、`[资料2]` 等，并在系统提示词中要求模型在相关句子或段落末尾嵌入来源引用，类似论文中的文内引用。
- **来源追踪仍保留在节点 meta** — 输出节点仍记录来源节点 ID、标题、类型、文件路径和引用标签，供导出 Markdown、历史追踪和后续功能使用。

## [2.2.2-alpha.26] — 2026-04-27

- **导出 MD 支持节点组展开** — 选中节点组导出时，会同时展开导出该节点组的成员节点，避免只导出一个空容器。
- **节点组导出说明更清楚** — 导出文件中会明确标注节点组容器语义，并提示成员节点已按画布位置展开。

## [2.2.2-alpha.25] — 2026-04-27

- **选中节点导出 Markdown** — 选中一个或多个节点后，悬浮工具条新增“导出 MD”，可把当前选区按画布位置合并导出为 Markdown 文件。
- **导出保留追溯信息** — 导出内容包含节点标题、类型、来源文件路径、正文 / 预览内容，以及 AI 输出节点的依据摘要和来源节点列表。
- **避免自动覆盖用户资产** — 导出通过 VS Code 保存对话框选择路径，默认不静默覆盖已有文件；图片、音频、视频、PDF 等非文本文件默认导出节点预览和文件路径，不强行读取二进制内容。

## [2.2.2-alpha.24] — 2026-04-26

- **节点级下一步建议** — 选中论文、笔记、AI 输出、图片、音频等节点后，画布悬浮工具条会显示 3-4 个轻量建议入口，例如内容摘要、文献综述、继续润色、语音转文字、图像编辑等。
- **建议只创建入口，不自动运行** — 点击建议只会在选区右侧创建对应功能节点，并把当前选中的数据节点连接过去；不会自动调用模型，保留用户确认和配置空间。
- **补建议规则与 Store 回归测试** — 新增建议生成单元测试，并锁定“从选区创建功能节点 + 批量连接 + 保持 idle 状态”的行为，避免下一步建议误变成自动执行。

## [2.2.2-alpha.23] — 2026-04-26

- **AI 文本输出追加依据说明** — 普通文本 AI 工具输出文件会自动追加 `## 依据说明`，列出本次连接到功能节点的输入材料、生成工具和模型推断提示；输出节点 meta 同步记录来源节点摘要，便于后续做下一步建议和导出追踪。

## [2.2.2-alpha.22] — 2026-04-26

- **AI 工具面板提示收口** — 多模态工具节点现在会显示更明确的调用提示：图像 / 视频标注为高成本调用，TTS / STT 标注为 AIHubMix 调用，并说明重复运行会保留新结果。
- **会议转写输出语义区分** — `会议转写` 不再只生成普通 Transcript，而是生成 `Meeting Transcript` Markdown，写入来源音频、模型、语言和会议转写语义说明，便于后续连接行动项 / 摘要 / 会议纪要工具。
- **运行 / 停止文案统一** — 高成本工具按钮改为“确认并运行”，倒计时取消显示为“取消确认”，运行中停止显示为“停止运行”，减少高成本调用和普通运行之间的交互歧义。

## [2.2.2-alpha.21] — 2026-04-26

- **补齐 AIHubMix live smoke 矩阵** — 新增 TTS、STT、Gemini 图像、Doubao 图像和视频 live smoke 脚本；脚本只读取本机 `AIHUBMIX_API_KEY`，不打印密钥、不保存生成文件。
- **视频 live 默认独立开关** — 视频 live smoke 需要显式设置 `AIHUBMIX_RUN_VIDEO_LIVE=1` 才会执行，避免误触发高成本视频生成。
- **修正 Gemini 图像默认模型** — live 验证发现 `gemini-3.1-flash-image-preview` 返回成功但不产出图像，默认图像生成 / 编辑模型改为已验证可出图的 `gemini-3-pro-image-preview`，并同步修正 Gemini 请求里的 `imageSize` 参数大小写。

## [2.2.2-alpha.20] — 2026-04-26

- **视频工具执行模块化** — 新增 `src/ai/multimodal/video-execution.ts`，将视频提交、图生视频 multipart、任务轮询、下载、取消感知与错误格式化从 Runner 主文件中迁出。
- **视频状态与失败原因更清楚** — 视频轮询状态会继续回传到功能节点进度；服务端失败时优先显示返回的失败原因，提交 / 查询 / 下载阶段的 HTTP 与网络错误也统一转成中文提示。
- **补视频执行单元测试** — 新增视频执行测试，覆盖文生视频 JSON、图生视频表单、轮询成功、下载结果、服务端失败原因、quota / 鉴权 / 网络错误提示。

## [2.2.2-alpha.19] — 2026-04-26

- **音频工具执行模块化** — 新增 `src/ai/multimodal/audio-execution.ts`，将 TTS / STT 的 AIHubMix 请求构造、HTTP 错误格式化、网络错误提示与返回解析从 Runner 主文件中迁出。
- **TTS / STT 错误提示收口** — 音频工具遇到 quota、鉴权失败或底层 `fetch failed` 时会显示更明确的中文说明，并保留 request ID / tid 或底层网络错误码便于排查。
- **补音频执行单元测试** — 新增音频执行测试，覆盖 TTS JSON 请求体、STT multipart 表单、json/text 转录返回、HTTP 错误和网络错误格式化。

## [2.2.2-alpha.18] — 2026-04-26

- **锁定 STT 输出稳定性** — 为 STT / 会议转写链路新增 Runner 回归测试，覆盖音频输入、AIHubMix 转录返回、输出节点和 `ai_generated` 连线持久化，确保转写结果只写入一个输出节点。
- **继续防重复节点回归** — 通过测试锁住 `.rsws` 中 STT 输出不产生重复节点 ID，避免后续保存恢复、连线和健康检查异常。

## [2.2.2-alpha.17] — 2026-04-26

- **新增 AIHubMix 图像 live 测试脚本** — 新增 `npm run test:aihubmix:image-live`，只在本机环境变量 `AIHUBMIX_API_KEY` 存在时运行，用于验证 `gpt-image-2` 的真实 `/v1/images/generations` 链路。
- **密钥不入库不打印** — live 测试只读取进程环境变量，不输出 token，不保存生成图像，脚本位于 `test/live/`，不会进入最终 VSIX。
- **完成一次真实链路验证** — 本机 live 测试返回 `200 OK`，检测到 1 个 base64 图像结果，确认当前 GPT Image 2 新链路可用。

## [2.2.2-alpha.16] — 2026-04-26

- **AI 工具运行防重入** — 同一个功能节点已有任务运行时，后续重复点击 Run 会被直接拦截并提示先等待或停止，避免高成本多模态任务被重复提交。
- **覆盖提交前窗口期** — 防重入现在从 run 请求进入宿主层就预留节点运行权，不再等到底层 AbortController 注册后才算运行中，减少图像 / 视频任务连点造成重复扣费的风险。
- **补 Runner 契约测试** — 新增功能节点运行预留与重复运行拦截测试，锁住同节点单运行语义。

## [2.2.2-alpha.15] — 2026-04-26

- **修正 GPT Image 2 请求链路** — GPT Image 生成从模型页 `predictions` 路由切到 AIHubMix OpenAI Images 兼容路由 `/v1/images/generations`，避免长耗时生成时上游已计费但 socket 被关闭后节点只显示网络失败。
- **修正 GPT Image 编辑提交方式** — GPT Image 编辑改用 `/v1/images/edits` 的 `multipart/form-data`，按 OpenAI Images 兼容接口提交参考图，不再把参考图塞进 predictions JSON。
- **补请求体回归测试** — 新增 GPT Image 生成请求体测试，锁定 `model/prompt/size/n/quality/moderation/background/output_format` 的顶层参数结构。

## [2.2.2-alpha.14] — 2026-04-26

- **图像工具网络错误提示优化** — AIHubMix 图像请求底层 `fetch failed` 现在会转成可读中文提示，说明可能是上游服务超时、DNS / 代理 / TLS 中断或长耗时生成连接被重置。
- **保留底层网络线索** — 节点错误里会附带 `UND_ERR_*`、`ETIMEDOUT` 等底层错误码或原因，方便区分本机网络、代理与上游服务波动。
- **补网络错误回归测试** — 新增图像执行网络错误格式化测试，避免后续又退回只显示 `fetch failed`。

## [2.2.2-alpha.13] — 2026-04-26

- **图像工具错误提示优化** — AIHubMix / GPT Image / Gemini / Doubao 图像接口的 HTTP 错误统一格式化，不再把整段 JSON 原样显示到节点里。
- **安全系统拒绝提示中文化** — 当 GPT Image 返回 safety system / policy 拒绝时，节点会提示“请求被安全系统拒绝”，并保留 request ID / tid 便于排查。
- **额度错误继续收口** — 图像工具里的 AIHubMix quota 不足会显示可读中文提示，减少用户看到原始 API JSON 的情况。

## [2.2.2-alpha.12] — 2026-04-26

- **图像工具输入更直接** — 图像生成新增可直接填写的“图像描述”，图像编辑新增可直接填写的“编辑指令”；仍兼容连接文本节点输入。
- **提示词构造收口** — 新增 `src/ai/multimodal/image-prompts.ts`，统一处理图像生成 / 图像编辑的直接参数与上游文本合并逻辑，避免 Runner 分支继续散写 prompt 拼接。
- **修正风格提示语义** — `style_hint` 只作为图像描述的补充，不再被单独当成图像主体，减少只填风格却误触发生成的情况。

## [2.2.2-alpha.11] — 2026-04-26

- **Gemini 图像执行模块化** — Gemini 图像生成与图像编辑请求已迁入 `src/ai/multimodal/image-execution.ts`，和 GPT Image / Doubao 共用图像执行模块。
- **Runner 继续瘦身** — `function-runner.ts` 只保留 Gemini 分支调度、运行状态、取消与结果回传，具体 endpoint、请求体、错误处理和结果持久化交给 multimodal 模块。
- **补 Gemini endpoint 测试** — `multimodal-image-execution.test.ts` 增加 Gemini 图像接口地址构造回归，锁定 AIHubMix Gemini 路由格式。

## [2.2.2-alpha.10] — 2026-04-26

- **图像请求执行模块化** — 新增 `src/ai/multimodal/image-execution.ts`，把 GPT Image 与 Doubao 的文生图、图像编辑、多图融合、组图输出请求体构造和错误处理从 `function-runner.ts` 迁出。
- **Runner 只保留图像工具调度职责** — `function-runner.ts` 现在主要负责输入校验、run 状态、取消和结果回传，具体 AIHubMix 图像请求由 multimodal 模块执行。
- **补执行参数测试** — 新增 `multimodal-image-execution.test.ts`，覆盖 GPT Image 张数限制、枚举参数归一化和参考图 data URL 构造。

## [2.2.2-alpha.9] — 2026-04-26

- **图像结果处理模块化** — 新增 `src/ai/multimodal/image-results.ts`，把 AIHubMix 图像返回解析、URL / data URL / base64 物化、图片文件落盘、输出节点创建与画布写回从 `function-runner.ts` 中抽出。
- **Runner 继续瘦身** — `function-runner.ts` 现在通过统一适配器持久化 GPT Image / Gemini / Doubao 输出，后续可继续把具体请求执行分支迁入 `src/ai/multimodal/`。
- **补图像返回解析测试** — 新增 `multimodal-image-results.test.ts`，覆盖 prediction payload 中 URL、base64、嵌套 images 等返回形态。

## [2.2.2-alpha.8] — 2026-04-26

- **梳理 AIHubMix 图像接入链路** — 明确 GPT Image、Gemini、Doubao 的路由、参数和返回处理，作为图像工具改造基础。
- **图像工具能力表首轮落地** — 新增 `src/core/aihubmix-image-models.ts`，集中管理 GPT Image / Gemini / Doubao 的模型族识别、endpoint 构造、尺寸归一化和参数可见性；Webview 参数面板改为读取能力表，减少 UI 内硬编码判断。
- **继续保持图像工具入口不合并** — 统一图像能力模型的同时，不改变现有“图像生成 / 图像编辑 / 多图融合 / 组图输出”入口，避免破坏旧画布和蓝图语义。

## [2.2.2-alpha.7] — 2026-04-26

- **图像生成 / 编辑新增 GPT Image 2** — 多模态图像生成与图像编辑工具新增 `gpt-image-2` 预设，走 AIHubMix `/v1/models/openai/gpt-image-2/predictions` 统一绘图接口，并支持 size、quality、moderation、background、output_format、n 等 GPT Image 参数。
- **按模型收口图像参数显示** — 图像工具会按 GPT Image / Gemini / Doubao 三类模型显示对应参数，避免 Gemini 看到 GPT Image 参数、Doubao 看到 Gemini 比例参数。
- **暂不合并图像工具入口** — 当前保留“图像生成 / 图像编辑 / 多图融合 / 组图输出”四个入口；它们的输入约束和输出语义不同，直接合并会让节点面板变复杂，后续更适合先做一个“高级统一图像工具”试验入口。

## [2.2.2-alpha.6] — 2026-04-26

- **补齐低风险修复的 store 集成测试** — 新增 `canvas-store-health-repair.test.ts`，覆盖设置面板触发的低风险修复进入 store 后会更新 `canvasFile`、ReactFlow 连线、节点组状态与选区。
- **锁住撤销与立即保存链路** — 测试明确断言修复前快照会进入撤销栈，修复后会发出 `canvasStateSync` 与 `canvasChanged`，同时干净画布不会产生撤销记录或保存消息。

## [2.2.2-alpha.5] — 2026-04-25

- **补齐画布健康检查单元测试** — 新增 `canvas-health.test.ts`，覆盖悬挂连线识别、低风险修复计划生成、悬挂连线移除、节点组缺失成员移除、节点组重复成员去重，以及干净画布保持不变。
- **锁住中风险不自动执行边界** — 测试明确覆盖节点组 hub 缺失与孤立 `group_hub` 只生成中风险计划，不会被低风险修复函数执行，防止后续误扩大自动修复范围。

## [2.2.2-alpha.4] — 2026-04-25

- **画布健康检查接入低风险确认修复** — 对悬挂连线、节点组缺失成员引用、节点组成员重复这三类低风险结构问题，新增“执行低风险修复”入口；执行前会弹出确认，执行后会写入撤销栈并立即同步保存。
- **中风险修复继续只预览** — 节点组 hub 重建、孤立 `group_hub` 移除等中风险动作仍只展示修复计划，不会自动执行，避免误删或误改用户可感知资产。

## [2.2.2-alpha.3] — 2026-04-25

- **画布健康检查新增修复计划预览** — 对悬挂连线、节点组缺失成员引用、节点组成员重复、节点组 hub 缺失、孤立 `group_hub` 等可安全规划的问题生成修复计划草案。
- **继续保持只读安全边界** — 本版本只展示拟修复动作与风险等级，不会自动执行、不修改 `.rsws`、不删除任何节点或连线；后续若接入一键修复，会继续要求用户明确确认。

## [2.2.2-alpha.2] — 2026-04-25

- **新增画布数据健康检查** — 在设置面板的“画布”详情中新增只读健康检查，可检测重复节点 / 连线 ID、悬挂连线、未知连线类型、异常节点尺寸、画板 bounds 异常、节点组 hub / 成员缺失、孤立 group_hub、暂存节点 ID 冲突，以及蓝图占位 / 实例绑定异常。
- **结构问题可见化但不自动修复** — 检查结果按错误、警告、提示分级展示，并统计节点、连线、画板、节点组、蓝图和暂存节点数量；本功能不自动删除、不自动修复任何用户资产，后续可在明确确认后再接入一键修复。

## [2.2.2-alpha.1] — 2026-04-25

- **新增文件转换环境诊断** — 在设置面板的“文档转换”详情中新增“转换环境诊断”，可一键检查 Word、PowerPoint、LibreOffice / soffice、PDF 渲染 runtime、MinerU 在线配置、本地 MinerU fallback 与表格转换能力。
- **降低转换失败排查成本** — 诊断结果按“可用 / 需注意 / 不可用 / 未检测”展示，并补充原因说明，便于用户判断 Word / PPT / PDF / MinerU 转换失败到底是缺少本机后端、Token 未配置，还是本地服务不可连接。

## [2.2.1-alpha.10] — 2026-04-24

- **优化多节点画布流畅度** — 拖拽、框选和节点选择期间不再把视觉态反复写回完整 `canvasFile`，避免几十个节点后触发全画布订阅重渲染；同时开启 ReactFlow 可视区裁剪，并把删除边清理从 `edge × node` 查找改为 Set 查询，降低多节点/多连线场景的掉帧概率。
- **新增缩放级别细节裁剪** — 画布缩小到几十个节点同屏时，自动进入 compact / minimal 显示层级：暂停 PDF 首页、图片、音频波形、Markdown、表格和功能节点参数区等富内容渲染，并在远视角隐藏 MiniMap；放大后恢复完整节点内容，减少“远看全局时还渲染所有细节”的性能浪费。
- **修正大范围缩放回弹** — 视口恢复逻辑不再在普通画布状态变化时重复套用旧 `canvasFile.viewport`，避免连续缩放时被拉回原视角。
- **低缩放改为摘要假状态** — compact / minimal 下不再让节点内容直接空掉，数据节点和功能节点会显示轻量摘要、类型、状态与骨架占位，保留全局识别感。
- **摘要态改为离散固定排版** — 取消连续 CSS 字号缩放，避免缩放每一帧触发大量节点文字重排；compact / minimal 各自使用固定精排样式，在保持节点边界、坐标和连线布局不变的前提下，提高标题、类型、状态和摘要的可读性。
- **LOD 切换后移并放大摘要字号** — full 模式保留到更远的缩放比例，避免过早摘要化；compact / minimal 摘要态标题和核心信息字号进一步放大，优先保证全局视角下能捕捉每个节点的核心内容。
- **画板 resize 角点保持屏幕恒定大小** — 画板边界的拖拽角点会按当前 zoom 反向计算尺寸，低缩放时仍保持可发现、可点击、可拖拽。
- **摘要态画板中心导航标签** — 画布进入 compact / minimal 后，画板中心会浮现大号标题标签，并通过独立上层 portal 显示在节点之上且不拦截鼠标事件，用于在全局视角下快速定位功能区。

## [2.2.1-alpha.2] — 2026-04-24

- **“文件爆炸”改为“文件转换”** — UI 文案统一改为文件转换，内部继续兼容旧 `explode-document` 工具 ID；旧画布和旧蓝图不需要迁移。
- **移除单独的 PDF 转 PNG 工具入口** — PDF 转 PNG 已并入“文件转换”的转换模式，AI Tools 不再保留重复的独立工具。
- **表格文件改为本地格式转换** — XLS / XLSX 不再进入 MinerU，可在功能节点上选择转换为 Markdown 表格或 TeX `tabular` 片段，并生成单个结果文件节点。
- **文档转换模式可选** — PDF / Word / PPT 可选择“仅转 PNG”或“拆解为文字 + 图片”；其中 PDF 直接逐页转 PNG，PPT 走 PPT → PDF → PNG，Word 走 Word → PDF → PNG。Word 转 PDF 现在优先使用本机 Microsoft Word，失败或缺失时回退 LibreOffice / soffice。
- **转换结果继续保留旧版本** — 同一文件重复转换时不覆盖旧结果，会新增一组转换结果，避免用户产出被自动替换。

## [2.2.1-alpha.1] — 2026-04-24

- **发布汇总版本：文件爆炸与删除确认主线收口** — 将 `v2.1.1-alpha` 后续开发成果汇总为新的发布节点：文件爆炸从 PDF 扩展到 DOCX、PPT/PPTX、图片、XLS/XLSX 等多格式主链，MinerU 在线解析、PPT 整页渲染、关系索引、AI 消费顺序、重复拆解结果保留与节点排布继续收口；同时画布、节点组、暂存架、任务项、设置项和宠物对话等删除 / 清空入口统一补齐确认层，降低误删风险。
- **发布仓库同步准备** — 版本号、README、CHANGELOG 与 GitHub Release Note 统一切到 `v2.2.1-alpha.1`，用于同步到 `research-space-release/` 后构建 VSIX 并作为 GitHub Release 资产发布。

## [2.1.1-alpha.12] — 2026-04-21

- **补上“外部改回旧 `.rsws` 后重开”的迁移集成测试** — 新增 `external-migration-reload.test.js`，覆盖“画布已打开时外部把 `.rsws` 改回旧结构 → 关闭并重开 → 旧 `summaryGroups`、残留 `group_hub` 与脏边自动迁移写回 → 再次重开仍稳定”的整条自定义编辑器链路，继续收口旧结构迁移不只要在 store 层通过、也要在 VS Code 宿主层真正成立

## [2.1.1-alpha.11] — 2026-04-21

- **修正 board 拖动后 nodeGroup / blueprint 叠加结构的持久化漂移** — 之前 board 移动节点时只改了可见节点位置，没有同步重算 `nodeGroups.bounds`，也没有顺带回收受影响的 blueprint 外壳；结果当前会话里看似正常，但重开画布后会被加载期归一化“再挪一次”。现在 board 移动会同步重算相关节点组 bounds，并回收受影响的 blueprint 容器位置，减少重开后的结构跳变
- **补上 board + nodeGroup + blueprint 叠加场景回归测试** — 新增 `canvas-store-overlap-persistence.test.ts`，直接覆盖“同一画板里同时包含节点组与蓝图实例，拖动画板后再重开”的稳定性，防止这条共享拖拽/持久化链路再次回归

## [2.1.1-alpha.10] — 2026-04-21

- **补上 nodeGroups / group_hub 旧迁移回归测试** — 新增 `canvas-store-group-migration.test.ts`，覆盖“旧画布已无节点组但还残留 `group_hub` / 边”、“历史 nodeGroups 成员列表含重复/失效节点”、“组框 bounds 需按真实成员重算”、“只保留合法 hub 边并立即写盘”等高回归场景，继续把旧 `.rsws` 迁移链路补成自动化护栏

## [2.1.1-alpha.9] — 2026-04-21

- **修正 `debouncedSave(..., 'immediate')` 只标脏不写盘的语义漏洞** — 之前不少“应立即落盘”的共享链路其实只做了内存同步和 pending 标记，没有真的发起保存；现在 immediate 路径会直接触发自动写盘，旧 `.rsws` 迁移、蓝图 definitions 回填等关键结构修正不再假性“已收口”
- **继续补迁移写盘回归测试** — `canvas-store-migration-regression.test.ts` 现在除了校验旧 `summaryGroups -> boards` 和隐式 output slot 合成结果，还会显式断言这些加载期迁移会立刻发出 `canvasChanged` 写盘消息，防止以后再次出现“内存里修好了，磁盘上没保存”的回归

## [2.1.1-alpha.8] — 2026-04-21

- **补上旧 `.rsws` 迁移与蓝图持久化回归测试** — 新增 `canvas-store-migration-regression.test.ts`，覆盖 `summaryGroups -> boards` 迁移稳定性、旧蓝图实例隐式 output slot 合成，以及 definitions 晚到时的输出链幂等性，防止“第一次打开正常、后续重开或 definitions 回填后又漂移”的回归
- **统一蓝图 definitions 迁移时的输出边语义** — 旧实例在 definitions 晚到后，不再额外补出一条 `function -> output placeholder` 的 `data_flow` 双连线；该路径现在和实例化链路一致，输出槽位前只保留正确的 `ai_generated` 支撑边

## [2.1.1-alpha.7] — 2026-04-21

- **package 专用构建开始先清空 dist** — 新增 `clean:package-dist`，让 `npm run build:package` 在无 sourcemap 构建前先删掉旧的 `dist/`；避免上一轮普通 build 残留的 `extension.js.map` 被 `vsce` 误带进最终 VSIX

## [2.1.1-alpha.6] — 2026-04-21

- **package 专用构建链路不再生成 sourcemap** — 新增 `build:ext:package / build:webview:package / build:package`，`npm run package` 现在会走无 sourcemap 的专用构建；保留日常 `npm run build` 的调试 sourcemap，但最终 VSIX 不再依赖 `.vscodeignore` 去“事后排除”这些包体垃圾
- **webview build 开始支持按环境变量开关 sourcemap** — `vite.config.ts` 现在读取 `RS_VSCODE_SOURCEMAP`，默认保持开发构建可调试，打包时显式关闭

## [2.1.1-alpha.5] — 2026-04-21

- **彻底收掉 VSIX 中遗留的 sourcemap** — 在 `.vscodeignore` 末尾补充 `**/*.map` 兜底排除，解决前一轮 `dist/**/*.map` 仍没拦住 `dist/extension.js.map` 与 webview sourcemap 的问题

## [2.1.1-alpha.4] — 2026-04-21

- **继续收口 VSIX 打包内容** — `.vscodeignore` 现在会额外排除 `dist/**/*.map`、`GITHUB_RELEASE_NOTE.md`、`eslint.config.cjs`、仓库 `.gitignore` 以及 webview 侧 `package*.json`；避免这些开发/发布辅助文件继续进入最终安装包
- **修正 `!dist/` 对 sourcemap 的反向放行** — 之前虽然全局写了 `*.map`，但后面的 `!dist/` 仍把 `dist/extension.js.map` 带回了 VSIX；现在改成在放行 `dist/` 之后再次显式排除 `dist/**/*.map`

## [2.1.1-alpha.3] — 2026-04-21

- **清零当前开发仓的 TypeScript 历史类型错误** — `npm run typecheck` 现已通过；这轮主要收口了 AIHubMix 模型上限缓存、Anthropic/Copilot provider 类型、legacy tool 索引、Blueprint 草稿构建类型守卫，以及 function-runner 多模态返回值与可选参数等一批长期堆积的类型漂移
- **共享节点类型守卫改为真正收窄到具体节点类型** — `isDataNode / isFunctionNode / isGroupHubNode / blueprint placeholder` 现在会把 `CanvasNode` 精确收窄到对应节点分支，减少 blueprint、pipeline、hub 展开链路里“明明有 title/id 却被 TypeScript 推成 never”的问题
- **蓝图实例转草稿时移除一段重复的 synthetic output slot 注入** — 之前这段残留逻辑既引用了不存在的变量，也会和后面的正式注入重复；现在保留单一路径，避免实例演化链路再出现重复输出槽位或运行期引用错误

## [2.1.1-alpha.2] — 2026-04-21

- **补上 Custom Editor 的 VS Code 层 undo / redo 端到端回归** — 新增 `undo-redo.test.js`，覆盖“打开画布 → 触发 provider 级编辑 → VS Code undo / redo → 保存回写文件”的完整主链路，避免 `CanvasDocument` 历史栈或自定义编辑器回调再次悄悄失效
- **provider 级 undo / redo 现在会同步回 webview** — 当 VS Code 触发自定义编辑器的撤销/重做回调时，恢复后的画布状态会立即重新发回当前 webview，避免宿主侧历史已回滚但画布 UI 仍停留在旧状态
- **补充内部测试命令用于集成测试观测** — 扩展现在注册隐藏的 `researchSpace.test.*` 命令，专供自动化测试读取当前活跃画布状态、注入最小 provider 级编辑，不进入命令面板，也不改变正常用户工作流

## [2.1.1-alpha.1] — 2026-04-21

- **发布汇总版本：收口自 `v2.1.0-alpha.41` 以来的蓝图主线改造** — 这不是单个小补丁版，而是把 `alpha.42` 到 `alpha.140` 期间已落地的有效改动统一汇总进新的发布节点：重点包括旧蓝图实例/旧画布严格迁移、蓝图 overlay 外壳与整实例拖拽、输出槽位历史与失败后继续、蓝图最终输出持久化/恢复链路、隐式 output slot 自动补齐、输出节点外置与重开不再掉线、输入/输出占位样式与自动测高收口，以及 Copilot 默认模型改为 `gpt-4.1`、创建蓝图弹窗文本选择误关修复、普通文件节点 footer 回归修复等。详细对比说明请见 GitHub Release Notes

## [2.1.0-alpha.140] — 2026-04-20

- **删除输入占位 footer 里重复的提示块** — 输入占位内部已经有“当前输入 / 接受类型 / 定位输入节点”等主信息，这轮把 footer 里重复出现的摘要提示、状态 chips 和定位按钮去掉，只保留更轻的槽位标题与当前输入状态，避免同一信息在节点里重复堆叠

## [2.1.0-alpha.139] — 2026-04-20

- **修复 alpha.138 占位节点底部边界无限下长的回归** — 上一轮为了把 footer 纳入 placeholder 自动测高，误把测量 ref 挂到了会跟节点当前高度一起伸展的容器上，结果测高链路把“当前高度”也当成了内容高度，触发持续增高。现在改回只测量占位卡片的自然内容骨架，并把 footer 一起纳入同一测量容器，既能覆盖多行底部提示，也不会再无限下长

## [2.1.0-alpha.138] — 2026-04-20

- **修复占位节点底部多行提示被遮盖** — 根因不在文案本身，而在 blueprint placeholder 的自动测高只测了 header + body，没有把 footer 算进去；当底部提示或状态说明变成两行以上时，节点高度不会跟着长，结果 footer 会被裁住。现在占位节点的自动测高已覆盖整张卡片内容，底部多行提示会随节点一起扩高，不再被遮盖

## [2.1.0-alpha.137] — 2026-04-20

- **输入占位继续按输出占位母版收口** — 这轮不再让输入/输出占位在 footer 里各走一套结构，而是直接把占位底部的槽位标题、当前状态、摘要说明、状态 chips 和定位动作统一为同一套 Placeholder Shell；输入占位仅替换为“接受类型 / 必填 / 多输入”等少量输入语义，避免继续出现“看起来像两种不同节点”的结构分叉

## [2.1.0-alpha.136] — 2026-04-20

- **输入/输出占位开始共用同一套 Placeholder Shell** — 这轮不再零碎补样式，而是把占位的标题区状态 badge、主体状态区、动作按钮入口统一到同一套骨架上；输入/输出现在共享相同的 shell，只保留“当前输入/当前输出”“定位输入节点/定位上游节点”等少量语义差异，从结构上收口“为什么总是看起来不像同一种占位节点”的问题

## [2.1.0-alpha.135] — 2026-04-20

- **修复蓝图创建弹窗里拖拽选中文字会导致弹窗误关闭的隐性 bug** — 根因不是蓝图草稿本身崩了，而是弹窗 backdrop 仍按“mouseup 后 click 落在遮罩上就关闭”处理；当你从输入框/说明文本里拖拽选中文字、鼠标释放到弹窗外时，就会被误判成点了遮罩。现在改成只有“mousedown 和 click 都明确起始于遮罩本身”时才关闭，避免文本选择把弹窗误关掉
- **Copilot 默认模型改为 `gpt-4.1`，设置页去掉“自动”模型项** — 这轮把 Copilot 的默认模型从空值/自动改成 `gpt-4.1`，让开箱即用直接落到免费可用模型；同时设置页的 Copilot 模型选择不再显示“自动”项，功能节点里若不单独覆盖，也会明确显示“跟随全局（gpt-4.1）”而不是再给一个含混的“自动”

## [2.1.0-alpha.134] — 2026-04-20

- **修复普通文件节点 footer 被内容区挤出边界的回归** — 根因不在文件节点自身，而在 `DataNode` 共享骨架：之前为 blueprint placeholder 自动测高加入的中间 wrapper 实际包住了所有节点，但只有占位节点走了特殊高度逻辑，普通文件节点这层 wrapper 没有 `flex: 1 / minHeight: 0` 约束，导致 header/body 会把底部 AI 提示 / 文本统计区挤到节点可视边界外；现在已把非占位节点的共享 wrapper 也补齐标准 flex 约束，让 footer 重新固定在节点内部

## [2.1.0-alpha.133] — 2026-04-20

- **输入占位外壳继续向输出占位收口** — 这轮直接改占位骨架本身：输入占位的箭头改为和整体左→右工作流一致的右向表达；同时去掉输入占位主体里那块仍显得过重的独立内卡，让主体信息区更接近输出占位的简洁状态表达，不再让输入占位看起来像另一种节点

## [2.1.0-alpha.132] — 2026-04-20

- **输入占位继续往输出占位的交互骨架收口** — 这轮不再只调卡片外观，而是把输入占位的主体状态表达改得更接近输出占位：主卡片现在直接显示当前输入状态，而不再像独立说明面板；同时补回“定位输入节点”按钮，让输入占位也具备更接近输出占位那种围绕槽位做定位/交互的入口

## [2.1.0-alpha.131] — 2026-04-20

- **输入占位样式继续向输出占位收口** — 输入占位不再维持那种独立的大说明卡 + 压缩 footer 的特殊版式，而是改回和输出占位更接近的标准占位结构：主体信息收成更轻的状态卡，底部摘要区恢复显示，整体更像稳定的语义锚点，而不是单独的信息面板

## [2.1.0-alpha.130] — 2026-04-20

- **继续修蓝图重开首帧跳变，这次直接统一容器 bounds 口径** — 之前虽然运行态的 blueprint 容器重算已经不再把外部 `blueprint_bound_*` 输出节点算进蓝图内部，但加载期 `normalizeBlueprintContainerBounds` 仍旧会把这些外部 bound 节点包进外框里，导致打开时蓝图先把真实输出包住，随后再在后续重算阶段缩回去，形成“输出没动、蓝图壳子跳了一下”的视觉跳变；现在初始化和运行态都统一只按真正的 `blueprint_instance_id` 内部成员计算容器 bounds

## [2.1.0-alpha.129] — 2026-04-20

- **蓝图输出首帧跳变继续往初始化链路前移** — 加载旧 `.rsws` 时，如果蓝图容器自己还没存下 `output_slot_defs`，现在会直接从实例内的 terminal function 结构合成隐式 output slot，再让后续 runtime-output 绑定恢复、placeholder 恢复和支撑边归一化都在首帧前吃到同一份槽位语义；这样像 `research.rsws` 这类旧测试画布，不必等 definitions 异步回填后再把真实输出从蓝图内搬到外侧
- **输入占位视觉开始向输出占位收口** — 输入占位的说明区改成更接近输出占位的紧凑状态卡，减少“说明大卡片”的堆料感；同时蓝图占位节点的输入/输出通道 hover 光环也统一加强，尽量把输入占位的通道观感拉齐到输出占位

## [2.1.0-alpha.128] — 2026-04-20

- **蓝图输出初始加载跳变继续收口** — 加载期的 `normalizeBlueprintOutputSupportEdges` 现在除了补输出支撑边，还会直接把已绑定的真实输出节点预先摆到 output placeholder 外侧；这样即使旧 `.rsws` 里还保存着蓝图内位置，首帧归一化也会先把它矫正到外侧，继续减少“刚打开先在蓝图内，1-2 秒后再跳出去”的视觉跳变

## [2.1.0-alpha.127] — 2026-04-20

- **蓝图输出拓扑继续改成“功能节点 → 输出占位 → 真实输出”** — 真实输出节点不再同时直接挂在功能节点和输出占位之间；运行时与宿主写盘现在都会优先把 `ai_generated` 连到 output placeholder，再由 output placeholder 连到真实输出节点，进一步对齐输入占位只做传递/锚点的设计语义
- **修复重开画布时输出先出现在蓝图内、随后再跳到外侧的闪动** — 宿主在写盘时就会按 output placeholder 外侧的位置落点真实输出，不再先按 placeholder 位置写进 `.rsws`，从而减少重开画布后 1-2 秒内从蓝图内跳到蓝图外的迟到校正

## [2.1.0-alpha.126] — 2026-04-20

- **蓝图真实输出开始固定落到蓝图外侧** — output placeholder 继续保留在蓝图内部作为稳定语义锚点，但运行产出的真实输出节点不再贴在 function 和 placeholder 中间，而是默认放到 output placeholder 右侧外部，并继续通过绑定边连回 placeholder
- **蓝图外框 bounds / 整实例拖拽不再把外部真实输出一起包进去** — blueprint 容器重算边界和标题栏拖拽现在只跟随真正的内部成员（`blueprint_instance_id`），不再把 `blueprint_bound_instance_id` 的外部输出节点也算成蓝图内部成员；这样真实输出会保持在蓝图外，而不是又被外框吞进去

## [2.1.0-alpha.125] — 2026-04-20

- **修复 alpha.124 打开画布即崩的回归** — `research.rsws` 路径排查过程中引入的隐式 output slot 恢复逻辑，误把 `normalizedDefinition` 变量带进了未声明作用域，导致 definitions 迁移阶段直接抛 `ReferenceError`；现在已改回在正确作用域里使用标准 `definition` / 局部归一化对象，画布可重新正常打开

## [2.1.0-alpha.124] — 2026-04-20

- **直接定位到 `research.rsws` 的根因：部分蓝图定义文件本身没有 output slot** — 这不是单纯“重开后裁边”那么简单；像 `简单测试.blueprint.json`、`画图1.blueprint.json` 这类测试蓝图，定义里只有 input / pipeline，没有任何 `output_slots`，所以运行出来的最终结果节点天然拿不到 blueprint output 身份，重开后当然也没有稳定槽位可挂
- **开始为“终点 function 没显式输出节点”的蓝图自动合成 output slot** — 无论是新建蓝图、基于实例回灌蓝图，还是旧蓝图 definitions 在加载期恢复时，只要检测到“某个终点 function 没有下游 pipeline、也没有显式 data/output 节点承载结果”，系统都会自动为它补一个隐式 output slot，并补对应的 function → output_slot 语义边；这样这类 blueprint 的最终结果终于有正式输出槽位可绑定和恢复

## [2.1.0-alpha.123] — 2026-04-20

- **继续补蓝图输出 placeholder 向外连线被旧白名单裁掉的问题** — 之前 definitions 迁移期的 runtime-support 判定只对白名单里的 input placeholder / bound node 放行，导致用户如果把蓝图输出槽位 placeholder 直接连到蓝图外，下次重开画布时这类 `data_flow` 外连线仍可能被旧裁边逻辑剪掉；现在 output placeholder 的合法外连线也会被视为 blueprint 正式支撑边保留下来

## [2.1.0-alpha.122] — 2026-04-20

- **继续拆蓝图重开后的旧裁边机制** — definitions 迁移阶段之前只把“碰到 blueprint 内部节点”的边当成待审核对象，某些已经写盘成功的 bound output 支撑边可能在后续迁移/清洗里又被当残留边剪掉；现在迁移阶段会把 blueprint bound output 也纳入正式结构审查范围，不再只按内部 placeholder / function 节点白名单裁边
- **加载期新增蓝图输出支撑边自修复** — 新增 `normalizeBlueprintOutputSupportEdges`：只要画布里还能识别出合法的 bound output、output placeholder 和来源 function，就会自动补回 `function -> bound output` 的 `ai_generated` 边，以及 `bound output -> placeholder` 的 `data_flow` 绑定边，继续收口“刚打开还有、归一化一跑又掉线”的旧链路

## [2.1.0-alpha.121] — 2026-04-20

- **蓝图最终输出重开后继续掉线，改成“稳定槽位骨架 + 显式绑定边”结构** — 单输出槽位运行时不再把 output placeholder 直接替换掉，而是始终保留输出占位骨架，只更新当前 bound output，并继续把旧当前输出上的外部连线重绑到新结果；这样重开画布后不再依赖临时重建“谁才是槽位本体”
- **宿主写盘开始把 output bound node → placeholder 的绑定边一起持久化** — Extension Host 把蓝图输出写回 `.rsws` 时，不再只落 `ai_generated` 边和 `blueprint_bound_*` 元数据，而是同步把输出节点连回真实 output placeholder；旧/新画布都能更稳定地直接恢复最终输出连线

## [2.1.0-alpha.120] — 2026-04-20

- **蓝图输出切换时不再把跨蓝图外连线一起剪掉** — 单输出槽位归一与运行态替换当前输出时，现在会把旧当前输出节点上挂着的外部连线、`ai_generated` 边和 placeholder 绑定边统一重绑到新当前输出，而不是在删除旧节点时把这些边顺手一起删掉；继续收口“蓝图向外连线运行几轮或重开后全部掉线”的结构问题
- **direct pipeline 的 blueprint runtime 中间结果开始按隐藏内部节点处理** — 对于 blueprint 内部 function 之间只用 `pipeline_flow` 直连、没有显式中间结果节点的链路，运行时生成的中间结果现在会以 `blueprint_runtime_hidden` 内部节点形式落盘和加载恢复，不再像普通输出一样弹到蓝图外；旧文件打开时也会尝试把这类 raw `ai_generated` 节点自动回补成隐藏内部 runtime 结果

## [2.1.0-alpha.119] — 2026-04-20

- **蓝图最终输出重开后掉线继续做结构修复** — 这轮不再只靠 webview 在 `aiDone` 后事后补状态；Extension Host 在把输出节点写回 `.rsws` 时，也会直接继承蓝图输出槽位的 `blueprint_bound_*` 元数据与基础蓝图身份，减少“磁盘先落一版裸 ai_generated 输出、之后 definitions 迁移把它当普通节点剪掉”的竞态
- **旧画布里的 raw blueprint 输出开始加载期自动回补绑定身份** — 新增一轮加载期修复：如果发现 blueprint 内部 function 的 `ai_generated` 输出落到了画布外普通节点上，但同函数能唯一反推出目标 output slot，就会把该节点回补成正式 `bound output`；从而继续降低“刚打开时线还在，definitions/归一化一跑就掉线”的旧文件残留问题

## [2.1.0-alpha.118] — 2026-04-20

- **修复蓝图最终输出在重开画布后误掉线的问题** — 加载期归一化现在遇到“蓝图槽位定义尚未水合”的实例时，会先保留已有的 `blueprint_bound_*` 输出/输入绑定元数据，不再抢先把当前输出节点当成无效残留清掉；同时“当前输出去重”也会等真实槽位 multiplicity 到位后再执行，减少重启后最终输出看起来从输出槽位脱离、或多输出实例被提前误降级的情况

## [2.1.0-alpha.117] — 2026-04-20

- **修复顶栏“画板”按钮二次点击无法关闭的问题** — 之前画板下拉打开后，再点同一个按钮时，外部点击监听会先把面板关掉，紧接着按钮点击又按新状态重新打开，造成“怎么点都关不上”；现在按钮被排除在外部点击关闭判定之外，二次点击会正常关闭面板

## [2.1.0-alpha.116] — 2026-04-20

- **输入/输出占位开始按内容自动扩高** — 占位节点在内容变化后会自动测量自身需要的高度，并同步更新节点尺寸与蓝图外框 bounds，不再因为信息稍多就直接超出边界、被裁掉或消失
- **占位边界适应性继续收口** — 这轮不再只靠固定尺寸和隐藏滚动条硬压布局，而是让占位边界跟随实际说明内容自适应，减少“去掉滚动条后文字又被吃掉”的回归
- **蓝图占位与外框联动更新** — 占位节点自动扩高后，会继续联动蓝图实例边界重算，避免占位节点本体虽然变高了，但蓝图外框仍停在旧尺寸

## [2.1.0-alpha.115] — 2026-04-20

- **输入占位节点去掉内部滚动条** — 输入占位现在改成紧凑静态布局，不再把内容区和底部摘要拆成两层滚动/重复信息区，避免节点本体里出现难看的滚动滑块
- **占位信息继续做减法** — 输入占位底部重复摘要区已移除，主要信息收敛为：顶部标题区 + 中部输入要求卡片；“当前绑定/等待连接”也合并回主卡片，不再上下重复说同一件事
- **占位提示改成更像“页面内说明卡”而不是“堆料面板”** — 说明文案做了压缩，绑定说明限制行数，减少越界、发黑和信息挤压感

## [2.1.0-alpha.114] — 2026-04-20

- **连接失败提示不再误混成“网络错误”** — 之前因为错误分类把“连接失败”里的“连接”字样误判进了 network 分支，导致槽位类型不匹配时也会套用网络错误标题和 Ollama 提示；现在连接校验失败会独立归类成“连接失败”，不再和网络提示混用
- **蓝图输入占位样式开始做减法收口** — 输入占位节点不再同时堆叠过多重复信息；“接受类型”保留为顶部主卡片，底部只保留当前槽位与绑定摘要，减少信息重叠、越界和视觉发黑发脏的问题
- **占位节点不再展示无意义的 AI 可读提示** — 蓝图输入/输出占位本身不是实际文件，不再继续渲染 AI 可读字数/全文长度之类的信息，降低占位节点底部噪音

## [2.1.0-alpha.113] — 2026-04-20

- **蓝图输入占位的“接受类型”提示改成前置强提醒** — 输入占位节点现在会在内容区顶部显式渲染“连接前请先看输入要求”卡片，把接受类型改成更醒目的类型胶囊，并同时显示必填/可选、单绑定/多绑定，避免用户还要到节点底部慢慢找一行浅提示
- **连错类型不再只有“连不上”的黑盒感知** — 连接拖拽结束但校验失败时，现在会按真实原因弹出明确提示，例如“这个槽位只接受图像，你拖入的是笔记文本”“当前槽位只允许 1 个输入”“请拖到蓝图输入槽位圆点上”等，减少用户把失败误判成系统不稳定
- **连接校验文案开始与实际规则共用同一套判断** — 画布实时 `isValidConnection` 与拖拽结束后的报错提示现在走同一套连接校验口径，避免“校验是一个规则、提示又是另一套模糊描述”的前后不一致

## [2.1.0-alpha.112] — 2026-04-20

- **旧蓝图 definitions 迁移开始统一“槽位骨架永远补回”语义** — 当旧实例只剩输入/输出 bound node、甚至同槽位有多个 bound output 时，迁移阶段现在会优先重建真实 placeholder 骨架，再把蓝图定义边重新挂回 placeholder，而不再把某一个 bound node 临时当成 slot 本体使用；这样多实例并存和半残旧结构恢复时，不容易再把输出槽位语义做散
- **多输出槽位与运行态支撑边在迁移期不再被误清** — definitions 迁移以前会把触及蓝图内部节点、但不属于定义原始边的边统统裁掉，容易误伤多输出槽位上的多个有效 bound output、外部输入绑定、以及 `ai_generated` 产物链路；现在迁移期会保留这些运行态支撑边，减少旧画布恢复后继续执行缓存、输入绑定和输出历史一起失联
- **单输出槽位 demote 旧输出时开始同步清理残留绑定边** — 当前输出归一化在把旧 output node 从“当前输出”降级时，现在会一并移除它连回 placeholder 的残留 `data_flow`，降低旧画布恢复后出现“UI 已只认一个当前输出，但画布上还挂着多条旧绑定边”的假多输出状态

## [2.1.0-alpha.111] — 2026-04-20

- **旧蓝图实例输出恢复继续补“真实产物边不要误改写”保护** — `normalizeLegacyBlueprintOutputBindings` 之前在把旧 output bound node 的内部槽位边回拉到 placeholder 时，可能把真实产物用的 `ai_generated` 边也一起改写，影响继续执行时对已有输出缓存的识别；现在这轮只会改写槽位语义的 `data_flow`，保留真实输出节点的产物链路
- **legacy 输入 / 输出槽位恢复开始统一限制在槽位数据边语义内** — 输入侧与输出侧的 legacy 归一化现在都只处理蓝图槽位自己的 `data_flow`，不再顺手碰到其它历史边类型，继续降低“为了修旧实例恢复，把真实运行链或历史输出链一并带坏”的回归风险

## [2.1.0-alpha.110] — 2026-04-20

- **旧蓝图实例恢复开始补“输出槽位缺失占位骨架”修复** — 这轮在加载期新增输出侧 legacy 归一化：如果旧画布里只剩输出 bound node、但丢了对应输出 placeholder，现在会自动补回输出占位骨架，并把实例内原本直连到旧输出节点的内部边重定向回 placeholder，避免旧实例恢复后输出槽位结构半残
- **输出槽位恢复口径继续向运行时填充语义对齐** — 对单输出槽位，旧残留的多个输出绑定现在会先按时间戳/节点 id 选出最新的当前输出，其余降级；同时只给当前输出补绑定边，减少旧画布恢复后“容器里看着有输出槽位，但内部仍沿旧输出节点直接挂边”的前后不一致

## [2.1.0-alpha.109] — 2026-04-20

- **蓝图引擎全局审查继续收口到“输出绑定口径统一”** — `BlueprintContainerNode` 与 `DataNode` 之前各自扫描输出绑定节点，且默认直接拿数组最后一个当“最新输出”；现在开始共用统一 helper，并按输出文件时间戳 / 节点 id 排序后再选最新项，减少旧画布加载顺序、节点顺序变化或多输出槽位下“定位的不是最新输出”的风险
- **蓝图输出槽位的计数与当前输出判定开始共用一套实现** — 输出数量统计、当前输出定位与 DataNode 输出状态判断现在不再各写一套相似逻辑，继续降低“一处修了多输出，另一处还按旧顺序取节点”的回归概率

## [2.1.0-alpha.108] — 2026-04-20

- **蓝图引擎全局审查后，先修复了“输入绑定统计前后不一致”问题** — 之前蓝图 Overlay / 容器节点在统计必填输入是否满足时，没有把“直接连到蓝图容器输入槽位”的 `data_flow` 计进去，导致 UI 可能错误显示“缺少必填输入”并阻止运行；现在输入绑定统计改成统一 helper，直接容器连线、占位连线与已绑定输入节点会按同一口径一起计数
- **蓝图输入绑定统计开始在 UI 侧共用同一实现** — `BlueprintOverlay` 与 `BlueprintContainerNode` 不再各自维护一套相似但不完全一致的统计逻辑，减少后续继续收口时再出现“一处修了、另一处漏了”的回归风险

## [2.1.0-alpha.107] — 2026-04-20

- **保存失败回执开始区分“当前失败”与“过期失败”** — 如果某次保存请求发出后，画布又被 file watcher 的外部持久化同步过，或者用户已经继续编辑到更新版本，旧 requestId 晚到的错误回执现在不会再把保存状态错误打成 `error`
- **in-flight save 与外部持久化的时序开始收口** — 当更旧的保存请求失败、但当前画布已经被更新版本覆盖、外部持久化接管或还有更新的保存请求在飞时，保存状态现在会回到 `saved / pending / saving` 的真实状态，而不是被陈旧错误卡住

## [2.1.0-alpha.106] — 2026-04-20

- **file watcher 不再只处理“最后一次 change”** — 之前 change 监听只保留最后一个 debounce 触发时的 URI，短时间内多个文件连续变化时，前面的预览更新可能被静默吞掉；现在改成批量聚合 pending URI，尽量把这一批真实文件变化都同步回画布
- **delete / create 监听开始做批量聚合与存在性确认** — delete 事件现在会先确认文件确实不存在，再批量标记节点缺失；create 也会从 pending delete 里撤销对应路径，减少 rename、原子写入或短时间成组文件变化时被误标记为 missing 的概率

## [2.1.0-alpha.105] — 2026-04-20

- **树视图删除节点不再默认回退到磁盘旧快照** — 侧边树执行“从画布移除”时，现在会优先读取打开中的 live canvas 状态，而不是直接拿磁盘上的 `.rsws` 快照改写，减少自动保存尚未落盘或 `canvasStateSync` 尚未追平时，把用户刚做的新改动一并覆盖掉的风险
- **导出 Markdown / JSON 开始优先使用当前 live canvas** — 画布已打开时，导出功能现在也会优先读取内存中的最新画布状态，减少“界面上已经改了，但导出仍落回上一次磁盘保存版本”的不一致

## [2.1.0-alpha.104] — 2026-04-20

- **蓝图 session 隔离继续补齐到手动刷新 / 草稿保存 / 蓝图删除链路** — `requestBlueprintIndex`、保存蓝图草稿后的索引回推、删除蓝图后的索引回推现在也会带上当前画布 session，减少“客户端已切到新 session，但后续索引刷新还沿用旧上下文”导致的误忽略或串写
- **树视图侧直接回推 init 时开始同步 bump 画布 session** — 从侧边树删除节点后，如果画布已打开，扩展侧回推 `init` 现在会携带新的 sessionId，避免 webview 自增 session 与扩展侧旧 session 脱节，继续收口蓝图恢复链路在非画布内操作后的不一致

## [2.1.0-alpha.103] — 2026-04-20

- **蓝图 definitions / 索引异步回填开始按画布会话隔离** — 切画布、重开画布或重新初始化同一张画布后，旧会话里延迟返回的 blueprint index / definitions 结果现在会被当前会话主动忽略，继续收口“上一张画布的蓝图定义晚到后又回写到当前画布里”的串写风险
- **蓝图恢复链路继续朝 R-01 / R-04 稳定性矩阵收口** — `init → blueprintIndex → requestBlueprintDefinitions → blueprintDefinitions` 这条链路现在会带上同一轮 session 标识，减少多实例并存、旧画布恢复和同路径重开时被陈旧异步消息污染的概率

## [2.1.0-alpha.102] — 2026-04-20

- **切画布 / 重开画布时的蓝图临时状态开始强制清空重建** — 初始化新画布时，现在会主动重置旧画布遗留的蓝图索引、输出历史弹窗、运行态、选区、撤销栈、预览状态和媒体 URI 缓存，减少“上一张画布的状态串到下一张画布里”的隐性回归
- **蓝图 definitions 自动补拉在重新初始化时会重新触发** — 之前自动补拉 definitions 只按文件路径 key 去重，重开同一张画布或切到另一张含相同蓝图路径的画布时，存在补拉被误跳过的风险；现在初始化时会重置这条去重键，降低旧画布恢复时 definitions 没重新到位的概率

## [2.1.0-alpha.101] — 2026-04-20

- **输出历史弹窗继续收口为“当前版 / 上一版 / 更早历史”分区** — 历史列表不再只是一整条平铺列表；现在会明确分区显示当前版、上一版与更早历史，更容易理解每一条记录在时间轴上的位置
- **历史弹窗补上快捷打开/恢复上一版** — 顶部摘要区现在可以直接打开当前版、打开上一版或恢复上一版，减少“知道上一版是谁了，但还得再往列表里找一遍”的来回操作

## [2.1.0-alpha.100] — 2026-04-20

- **输出历史弹窗开始正式标出“当前版 / 上一版 / 历史”** — 历史列表里不再只是简单按文件倒序平铺；现在会明确标出当前版本与上一版本，帮助用户更快判断“这一版之前是什么”
- **蓝图槽位历史补上更正式的摘要语义** — 弹窗顶部会直接显示当前槽位输出数量、上一版文件名与总历史条数；上一版条目本身也会给出更明确说明，开始把蓝图输出历史从“能看”继续推进到“更容易对比和恢复”

## [2.1.0-alpha.99] — 2026-04-20

- **蓝图输出槽位开始正式尊重“多输出”配置** — 当输出槽位勾选了“允许多输出”后，新结果不再一律把旧结果降级成历史残留；同一槽位现在可以持续保留多个当前输出节点，行为终于和槽位配置本身一致
- **单输出 / 多输出语义进一步收口** — 单输出槽位仍保持“一个槽位一个当前输出、旧结果自动退历史”的口径；多输出槽位则改为“新结果追加、旧结果继续保留”，减少重复运行后输出行为前后不一致的问题

## [2.1.0-alpha.98] — 2026-04-20

- **槽位级错误高亮继续下沉到真实节点层** — 蓝图输入占位在“必填但未绑定”时，现在会直接带警告态边框与高亮；蓝图输出占位在“等待产出 / 失败未产出 / 取消未产出”时，也会直接在节点本体上显示更明确的状态，而不再主要依赖隐藏容器里的摘要信息
- **输出槽位节点开始直接联动关联上游节点** — 当某个输出槽位受上游失败或仍在等待上游链路推进时，对应占位/回填节点会直接显示关联节点信息，并提供一键定位入口，进一步把“哪个槽位出了问题”收口到用户真正能看到的节点上

## [2.1.0-alpha.97] — 2026-04-20

- **蓝图实例开始支持“基于当前状态另存为新蓝图”** — 右键蓝图 Overlay 现在可直接把当前实例内部功能节点、内部数据节点与当前槽位布局回灌成新的蓝图草稿，不必再回到原定义手工重建一份变体
- **实例演化草稿默认按副本标题避开同名冲突** — 从实例发起的新蓝图草稿会自动补成 `- 副本` / `- 副本 2` 之类的唯一标题，减少“当前实例想保存成新模板，但一打开弹窗就先撞名”的阻塞

## [2.1.0-alpha.96] — 2026-04-20

- **蓝图编辑弹窗补上“另存为新蓝图”** — 编辑已有蓝图时，不再只能原地覆盖保存；现在可以一键按当前内容生成新的蓝图副本，便于做模板分支演化
- **另存为新蓝图会自动避开同名冲突** — 若当前标题仍等于原蓝图标题，另存为新蓝图时会自动补 `- 副本` / `- 副本 2` 之类的唯一名称，避免用户还得先手动改名才能保存出新模板

## [2.1.0-alpha.95] — 2026-04-20

- **已保存蓝图回编辑首轮落地** — 蓝图库条目现在补上 `✎` 编辑入口，可直接把已有蓝图定义重新载入创建/编辑弹窗，不必再从头重建一份同结构蓝图
- **编辑蓝图支持原地覆盖与改名保存** — 编辑已有蓝图时，保存会默认覆盖原蓝图；若同时修改名称，则会按新名称生成新的蓝图文件名并清理旧文件，重复名称校验也会自动忽略当前正在编辑的同一蓝图

## [2.1.0-alpha.94] — 2026-04-20

- **继续加固功能节点自动高度同步** — 在 alpha.93 去掉缩放污染后，这次进一步给功能节点高度同步补上 RAF 去重、挂起测量取消和“最近一次自动高度”保护，减少重载 / 点击 / ResizeObserver 连续触发时的重复回写与抖动
- **功能节点边界异常根除方向继续收口** — 现在即使同一节点在短时间内被多次测量，也会尽量避免旧测量结果回头覆盖新状态，降低底部长空白再次回归的概率

## [2.1.0-alpha.93] — 2026-04-20

- **继续修复功能节点重载后边界异常 / 底部长空白** — 功能节点自动高度计算之前混用了 `getBoundingClientRect()`，在画布缩放后的 React Flow 变换下会把视口缩放一起算进节点高度，导致重载画布、重开插件或再次点击后节点高度容易漂移变大；现在改为只使用不受缩放影响的布局高度指标（`scrollHeight / clientHeight / offsetHeight`）
- **功能节点高度回归更稳定** — 这次修复后，功能节点的自动高度不应再随着画布缩放、重载或普通点击而反复出现“运行按钮到底部一大块空白”的异常

## [2.1.0-alpha.92] — 2026-04-20

- **蓝图 Overlay 补专属搜索高亮样式** — 搜索命中蓝图标题时，Overlay 外壳现在会直接显示金色边框、高亮标题栏和命中徽标，不再只能靠普通节点层的通用搜索样式去猜测命中状态
- **当前命中的蓝图会有更明显的动效提示** — 当前搜索命中的蓝图 Overlay 会显示更强的金色高亮与柔和脉冲，未命中的其它蓝图 Overlay 会轻度降权，搜索蓝图时更容易一眼看出当前焦点

## [2.1.0-alpha.91] — 2026-04-19

- **修复搜索蓝图标题时跳到空白区域** — alpha.90 虽然修掉了查找时蓝图双层边框，但搜索定位仍直接对着隐藏的蓝图容器节点做 `fitView`，结果会跳到空白位置；现在若命中的是蓝图容器，会自动改为对同实例的可见成员节点做定位
- **蓝图搜索定位继续匹配 Overlay 视角** — 搜索蓝图时，定位逻辑现在更接近用户真实看到的蓝图实例范围，而不是隐藏实现层的容器节点

## [2.1.0-alpha.90] — 2026-04-19

- **修复查找时蓝图出现双层边框** — 搜索高亮之前会把本应保持隐藏的蓝图实例容器节点错误重新显出来，于是和 Overlay 外壳叠在一起，表现成一层黄框套一层蓝图框；现在搜索态会继续保留原有 hidden 节点，不再把隐藏容器错误显示出来
- **蓝图“双层”现象根因已收口** — 蓝图本身确实是“隐藏容器节点 + Overlay 外壳”双层机制，但正常情况下隐藏容器不应露出来；本次修复后，查找场景下也只保留用户应该看到的那一层显示外壳

## [2.1.0-alpha.89] — 2026-04-19

- **蓝图自动排版继续压输入列高度** — 输入占位现在不再严格跟随全局 row pitch，而会按更紧凑的输入列专用纵向分布规则排版，减少输入占位框之间上下间隔过大的情况
- **输入列仍尽量贴近对应功能节点** — 这次只是压缩输入列内部的垂直空白，不是简单硬堆在一起；输入占位仍会尽量贴近各自消费功能节点所在位置，兼顾紧凑和可读性

## [2.1.0-alpha.88] — 2026-04-19

- **蓝图自动排版继续压输入列** — 输入槽位列现在不再按原始选区里文件节点的宽度直接撑开，而是按蓝图实例真实输入占位的有效宽度参与排版，减少“输入列本体不大，但左侧仍留了一大块空宽”的情况
- **输入列到功能列的横向占位更贴近真实显示** — 保存后的蓝图定义会直接把输入槽位宽高收口到实例占位的展示口径，让后续实例化时左侧结构更紧凑、更接近最终真实视图

## [2.1.0-alpha.87] — 2026-04-19

- **蓝图自动排版改为只压横向** — 在 alpha.86 继续压紧的基础上，保留更紧凑的列间距，但把默认行高与垂直留白恢复到更稳的口径，避免整体同时横纵都过度压缩
- **多分支布局更容易读** — 现在蓝图实例会优先朝“横向更紧、纵向仍留足层次”的方向收口，减少分支较多时因为上下也过紧而显得拥挤的情况

## [2.1.0-alpha.86] — 2026-04-19

- **蓝图自动排版在“更工整”基础上继续压紧** — 输入列、功能列、中间结果列和输出列之间的默认列距进一步收小，减少上一版虽然工整但横向铺得过开的情况
- **行距也同步收口** — 自动排版的默认行高和垂直留白一起压缩，让多分支 pipeline 在保持对齐感的同时整体更紧凑，不容易一保存就摊得很高

## [2.1.0-alpha.85] — 2026-04-19

- **蓝图保存期排版切到“更工整”方案** — 功能节点现在会按 pipeline 层级落到更明确的列上，并尽量沿上游链路对齐到统一行，减少上一版那种“虽然比原图整齐，但还不够像标准流程图”的情况
- **输入 / 中间结果 / 输出对齐感继续增强** — 输入占位、内部中间结果和输出占位都会尽量贴合对应功能节点所在行，并统一列距与行距，让实例化后的蓝图内部结构更像规则化的 pipeline 视图

## [2.1.0-alpha.84] — 2026-04-19

- **蓝图保存弹窗新增“保存时优化内部布局”开关** — 创建蓝图时现在可以选择是否在保存前自动整理内部排布，默认开启；这次整理只作用于将要保存的蓝图定义，不会改动当前画布上的原始节点位置
- **蓝图定义首次支持保存期标准化排版** — 默认会把输入占位、功能节点、中间结果和输出占位按 pipeline 分层重新布局，让新实例化出来的蓝图内部结构更规整，减少“保存前原始选区排得乱，蓝图实例里也跟着乱”的情况

## [2.1.0-alpha.83] — 2026-04-19

- **蓝图边界改成跟随真实拖拽位置实时重算** — 之前内部节点直接拖动时，隐藏容器仍可能拿旧的 `data.position` 计算 bounds，导致外框不跟着成员节点即时更新；现在改为按 React Flow 当前节点位置/尺寸实时重算，减少“节点已经移走，但边界还停在原地”的问题
- **蓝图实例加载不再拿输出结果节点反推整体原点** — 旧逻辑会把 `blueprint_bound_*` 输出结果节点也当成实例锚点，重开画布时有机会把整组内部节点错位平移；现在只使用蓝图内部占位 / 功能节点 / 数据节点来恢复原点，收口“刚打开画布节点初始位置就移位”的回归

## [2.1.0-alpha.82] — 2026-04-19

- **蓝图边界继续补实时自适应** — 蓝图实例整体拖动、内部节点尺寸变化后，现在也会立即重算隐藏容器 bounds，减少“节点/占位框已经移走，但蓝图边界还停在旧位置”的情况
- **蓝图中间结果标题改为专属语义名** — 新建蓝图时，由功能节点产出的中间结果 / 输出结果不再沿用普通 AI 输出节点的时间戳标题，而会优先收口成 `中间结果 · 功能节点名` / `输出结果 · 功能节点名`

## [2.1.0-alpha.81] — 2026-04-19

- **蓝图占位框不再沿用原文件节点尺寸** — 输入 / 输出占位现在统一改为更稳定的专属尺寸，不再因为原始文件节点过大而把占位框也撑成大块卡片
- **蓝图占位框改成更明确的槽位样式** — 占位框标题不再直接复用原文件节点名，而是统一显示 `输入占位 / 输出占位`，原始语义名称下沉为“槽位语义”辅助信息，视觉上更像蓝图专属槽位而不是普通文件节点复制品

## [2.1.0-alpha.80] — 2026-04-19

- **功能节点服务商 / 模型下拉框宽度收口** — 模型下拉之前因为包在额外容器里，`select` 本体没有被强制拉满，宽度会跟着当前选中模型文本忽大忽小；现在服务商和模型下拉都改为固定撑满同一列宽度，和服务商控件保持一致

## [2.1.0-alpha.79] — 2026-04-19

- **AI 工具栏蓝图条目补上删除入口** — 蓝图库列表里的每个蓝图现在除了“直接加载到画布”外，也补了显式删除按钮，避免新建蓝图后只能去工作区目录里手动删文件
- **删除蓝图前补确认提示** — 删除时会提示当前画布里是否还有实例引用该蓝图文件，避免用户误删后才发现旧实例后续无法再从定义恢复

## [2.1.0-alpha.78] — 2026-04-19

- **AIHubMix 常用 Gemini 再排除 `customtools` 变体** — 之前虽然已经排除了 `-search`，但默认常用模型匹配仍会因前缀命中把 `gemini-3.1-pro-preview-customtools` 识别成 Gemini Pro 主模型；现在已显式排除 `customtools` 后缀，继续只保留标准 Gemini Pro 主模型

## [2.1.0-alpha.77] — 2026-04-19

- **AIHubMix 常用 Gemini 模型不再命中 search 变体** — AIHubMix 的默认常用模型匹配现在会优先锁定 `gemini-3.1-pro-preview` / Gemini Pro 主模型，并显式排除 `-search` 变体，避免文本功能节点默认常用模型里把 Gemini 搜索版塞进来

## [2.1.0-alpha.76] — 2026-04-19

- **旧蓝图输入替换结构开始自动迁回“占位 + 连线”** — 打开历史画布时，若旧蓝图实例里还残留输入节点直接接管槽位的旧结构，现在会自动补回输入占位，并把旧的内部直连关系回迁成“外部节点 → 输入占位 → 蓝图内部”的连线语义
- **旧输入接管节点不再长期卡在槽位位置** — 对于历史上被吸附到占位位置的旧输入节点，迁移时会自动解除输入绑定元数据，并在需要时轻微挪开，避免补回占位后两张卡片继续重叠

## [2.1.0-alpha.75] — 2026-04-19

- **移除蓝图输入占位“拖拽重叠替换”逻辑** — 输入占位不再尝试通过节点重叠自动替换/删除占位框，避免继续保留一条一直不稳定、也不如直接连线好用的交互分支
- **蓝图输入占位统一收口为“连线传递”** — 创建蓝图、占位提示和占位卡片描述现在统一改成“直接连到输入占位”，并会在占位框内优先显示当前已连接的上游节点描述

## [2.1.0-alpha.74] — 2026-04-19

- **蓝图标题栏呼吸动效改得更柔和** — 运行 / 继续按钮的脉冲动画改为更长时长、更平滑的 easing，并拆成多段关键帧，减少之前那种“亮是亮了，但呼吸有点僵”的感觉
- **按钮浮动感收敛** — 轻微上浮和放大幅度都进一步收小，保留醒目提示，但不再有那么明显的突兀跳动感

## [2.1.0-alpha.73] — 2026-04-19

- **蓝图标题栏运行按钮继续增强对比度** — `运行` 按钮改为更亮的蓝色渐变主按钮，`继续` 改为更亮的橙黄渐变强调按钮，进一步拉开和蓝图边框的色差，减少“有动效但还是不醒目”的问题
- **按钮高亮加入更强发光与内高光** — 运行 / 继续 / 停止按钮现在都补了更明显的阴影、内高光和更强的脉冲光晕，标题栏里可执行入口会更容易第一眼被看到

## [2.1.0-alpha.72] — 2026-04-19

- **蓝图标题栏运行按钮对比度进一步增强** — `运行` 按钮改为更高对比度的亮蓝主按钮，`继续` 按钮改为亮黄强调按钮，不再和蓝图边框颜色过于接近
- **运行/继续动效分离** — `运行` 使用蓝色脉冲，`继续` 使用黄色脉冲，进一步降低“按钮在动，但还是不够醒目”的问题

## [2.1.0-alpha.71] — 2026-04-19

- **蓝图运行/继续按钮改为高亮动效提示** — 当蓝图实例具备可运行条件时，标题栏里的 `运行 / 继续` 按钮会进入更醒目的脉冲提示，提醒用户当前可以直接执行
- **蓝图执行入口补上“2 秒后悔期”** — 点击 `运行` 或 `继续` 后，不会立刻发起蓝图执行，而是先进入 2 秒倒计时；这段时间按钮显示 `取消 (2s/1s)`，用户可直接撤销本次启动
- **蓝图运行中按钮统一改为“停止”** — 超过 2 秒后正式发起蓝图执行，标题栏按钮切为 `停止`；点击后会终止当前蓝图 pipeline，行为与之前功能节点的双阶段取消/停止需求保持一致

## [2.1.0-alpha.70] — 2026-04-19

- **蓝图 Overlay 标题栏补回运行入口** — 运行 / 继续 / 取消按钮现在直接回到蓝图标题栏上，不再因为切到 overlay 外壳后丢失实例级执行入口
- **蓝图边界重新跟随内部节点拖动实时自适应** — 当实例内部 pipeline 节点移动时，隐藏容器会重新按成员节点计算边界，蓝图外框会继续包住内部节点，不再默认允许节点长期拖出外框之外

## [2.1.0-alpha.69] — 2026-04-19

- **继续修复蓝图 Overlay 的 React #185** — alpha.68 只把 selector 内的 `filter` 拿出去了，但在 `canvasFile` 尚未到达时仍用 `?? []` 返回了新的空数组引用，快照仍不稳定；现在改为直接订阅 `canvasFile?.nodes` 原始引用，组件内部再用稳定常量兜底，进一步消除 mount 阶段的快照抖动
- **BlueprintOverlays 空态回退改为稳定引用** — 在没有画布节点时，不再给 zustand selector 返回新的 `[]`，避免重启窗口后打开画布时再次触发 Render Error

## [2.1.0-alpha.68] — 2026-04-19

- **修复蓝图 Overlay 在重启后打开画布时触发的 React Render Error** — `BlueprintOverlays` 之前直接在 zustand selector 里 `filter` 生成新数组，导致 `useSyncExternalStore` 在挂载阶段反复判定快照变化，进而触发 React #185；现在改为先订阅稳定的 `canvasFile.nodes` 引用，再在组件内 `useMemo` 过滤蓝图实例，消除这条渲染回路
- **蓝图 Overlay 订阅链改为稳定快照** — 这次修复不会改变蓝图 Overlay 的主机制方向，仍沿用 alpha.67 的画板式外壳方案，只是把导致重启后直接 Render Error 的订阅实现收稳

## [2.1.0-alpha.67] — 2026-04-19

- **蓝图显示层开始正式切到画板式 Overlay 机制** — 蓝图实例外壳不再主要依赖 React Flow 里的可拖拽容器节点来承担显示与交互，而是改为用独立 overlay 渲染标题栏与边框；内部原始 pipeline 节点继续作为主视图主体
- **蓝图容器节点开始退出主视图命中链路** — 蓝图实例容器节点现在默认隐藏，不再直接参与主画面的点击与拖拽命中；这一步是为了解决“边界二次收窄、误拖内部节点、拖动延迟”这类由节点式容器机制带来的耦合问题
- **蓝图 Overlay 拖拽改成快照式整实例平移** — 拖标题栏时，会先快照当前实例成员，再像画板一样按 overlay 拖拽批量平移整组蓝图成员节点，继续向“蓝图只是实例外壳”收口
- **实例容器同步不再强制回退到槽位计数尺寸** — 旧蓝图 definitions / index 同步过程中，实例容器不再被统一 size 归一逻辑拉回固定槽位尺寸，减少“刚算对又被收窄”的后续回退

## [2.1.0-alpha.66] — 2026-04-19

- **旧蓝图容器边界开始在加载期强制按成员节点重算** — 打开旧画布时，蓝图容器不再只依赖历史保存的外框尺寸；现在会按当前实例成员/绑定节点重新计算边界，减少“刚打开时边框溢出，拖一下才恢复”的迟到收口问题
- **蓝图拖拽时不再每帧重算容器外框** — 容器拖动过程中，蓝图实例成员仍会一起移动，但容器 bounds 的重算改为避开拖拽中的每帧刷新，只在非拖动态收口，减少拖动时的延迟与粘滞感
- **蓝图标题栏命中再收紧一轮** — 标题栏按下时会先收起上下文菜单并显式选中蓝图容器，继续提高“拖标题就是拖蓝图外壳”的命中稳定性

## [2.1.0-alpha.65] — 2026-04-19

- **蓝图容器改回极简外壳** — 按当前蓝图使用口径，容器内部不再额外渲染“输入槽位 / 实例状态 / 输出槽位”三栏摘要，避免与实例内部真实 pipeline 节点形成双层信息混叠；当前蓝图容器只保留外框与标题栏，让画布主视图重新回到原始节点本身
- **蓝图标题栏拖拽优先级收口** — 蓝图容器层级已抬回到内部节点之上，但主体改为不拦截指针，只有标题栏保留交互；这样拖动标题栏时会稳定命中蓝图外壳，不再出现“想拖蓝图，实际拖走了内部 pipeline 节点”的错位行为
- **蓝图容器继续向“仅作为实例外壳”收口** — 当前蓝图主视图不再强调重复摘要，而是把蓝图容器退回成边界与标题语义，后续若还要补更多状态信息，应优先走不打扰主视图的轻量路径

## [2.1.0-alpha.64] — 2026-04-19

- **definitions 批量读取失败不再一锅端** — 旧画布恢复蓝图 definitions 时，若其中某几个蓝图文件已被移动、删除或损坏，宿主不再让整批 definitions 请求一起失败；其余仍可读取的蓝图实例会继续正常恢复
- **缺失蓝图文件开始给出更具体的恢复提示** — 当批量 definitions 请求里有个别蓝图文件失败时，界面会提示具体失败的蓝图文件名，并明确说明“其余实例已继续恢复”，帮助区分局部蓝图损坏与整画布恢复失败
- **R-04 继续向“局部损坏可降级恢复”收口** — 旧画布里混有失效蓝图文件时，恢复链路开始支持部分成功、部分失败的降级行为，避免一个坏蓝图拖垮全部旧实例恢复

## [2.1.0-alpha.63] — 2026-04-19

- **旧画布打开后会主动补发蓝图 definitions 请求** — 不再只等蓝图库索引消息触发；只要当前画布里存在蓝图实例且带有 `blueprint_file_path`，前端就会主动请求对应 definitions，减少因消息时序或旧文件状态导致 definitions 没跟上、实例恢复不完整的情况
- **definitions 现在也能直接回补容器元数据** — 即使蓝图库索引条目还没来或暂时不可用，definitions 迁移本身也会直接把容器标题、版本、颜色、槽位数量与槽位定义补回去，降低恢复链路对 index 命中的单点依赖
- **R-04 继续向“弱依赖 index、强依赖 definition 自恢复”收口** — 旧画布蓝图实例恢复现在进一步从“等索引、再迁移”推进到“只要 definition 到达就能先恢复主体结构”，为后续专项回归矩阵继续铺路

## [2.1.0-alpha.62] — 2026-04-19

- **旧画布蓝图恢复开始补“二次归一化收口”** — 蓝图库索引回填到旧画布容器后，不再只更新容器卡片本身；现在会立刻再走一轮统一归一化，把刚拿到的槽位定义继续同步到旧占位节点、旧绑定节点和当前输出绑定状态，减少“容器元数据已新、实例骨架还旧”的半迁移窗口
- **definitions 到达后的迁移结果不再直接原样落盘** — 蓝图 definitions 驱动实例补齐/清理后，现在会再次经过同一条运行期归一化链，再写回 `.rsws`，把 definitions 迁移与加载期清理收成同一口径，进一步降低旧实例恢复后还残留脏结构的概率
- **R-04 首轮收口开始接到真实加载链路里** — 旧画布蓝图实例恢复不再只依赖单点修补，而开始把“索引水合 → definitions 迁移 → 再归一化”串成连续闭环，为后续专项回归验证打基础

## [2.1.0-alpha.61] — 2026-04-19

- **旧蓝图实例开始按容器元数据做更严格的加载期修正** — 打开旧画布时，蓝图成员节点会先对齐所属容器当前保存的槽位定义与蓝图元数据：占位节点会补齐标题、accepts、必填/多绑、替换模式与提示，内部节点也会补齐 `blueprint_def_id / blueprint_color`
- **容器内非法残留开始更早清掉** — 若旧实例里残留了容器已不再认识的占位节点，或绑定到已不存在槽位的旧 `blueprint_bound_*` 元数据，现在会在 definitions 正式迁移前就先清掉/解绑，减少“结构半恢复、槽位元数据还停在旧状态”的窗口期
- **C2-01 继续向“加载即高置信度恢复”收口** — 蓝图实例恢复不再完全依赖后续 definitions 到达后再修；现在会先利用容器自身元数据完成一轮更严格的高置信度归一

## [2.1.0-alpha.60] — 2026-04-19

- **继续执行开始明确提示复用了多少上游缓存** — 当蓝图实例以“失败后继续”模式运行时，Pipeline 启动态和容器当前步骤区都会带上已复用成功上游节点缓存的数量，帮助用户判断这次到底是不是从失败点继续而不是偷偷全量重跑
- **运行历史开始记录缓存复用情况** — `blueprint_run_history` 里的继续执行记录现在会额外写入本次复用了多少成功上游缓存，方便回看
- **拒绝态的运行模式记录更准确** — 蓝图运行在入口阶段被拒绝时，也会尽量保留本次本来想发起的是 `全量执行` 还是 `继续执行`，减少历史记录口径漂移

## [2.1.0-alpha.59] — 2026-04-19

- **蓝图实例开始持久化独立运行历史** — 每次蓝图运行完成或被拒绝后，容器元数据里都会追加一条历史记录，至少包含时间、状态、摘要、完成数、失败数、跳过数、警告数以及问题节点信息
- **容器内补上实例运行历史面板** — 蓝图容器状态区现在会直接显示最近几次运行记录，并区分 `全量执行 / 继续执行`，帮助用户回看这个实例最近到底发生过什么
- **D1-02 首轮落地** — 实例级运行历史不再只剩“最近一次摘要”；蓝图容器已开始具备一条轻量但持续累积的历史序列

## [2.1.0-alpha.58] — 2026-04-19

- **蓝图实例新增“失败后继续”入口** — 当最近一次蓝图运行失败且能定位到实例内失败节点时，容器状态区会出现 `↻ 失败后继续` 按钮，开始落地 2A 语义
- **继续执行开始按“失败节点 + 下游链路”重跑** — 本轮继续执行会保留完整蓝图执行计划，但把失败节点及其下游重新置为待运行；其余已成功上游会优先视为缓存，不再默认从头全量重跑
- **成功上游开始预装入 Pipeline 缓存** — 继续执行前，宿主会尝试从当前画布上读取这些已成功上游节点最近一次 `ai_generated` 输出，并预先注入到 Pipeline 上下文，帮助失败点后的节点直接复用已有结果

## [2.1.0-alpha.57] — 2026-04-19

- **蓝图输出历史开始正式按“槽位”解释** — 从蓝图输出槽位点击“历史”时，宿主会带上实例/槽位信息，历史弹窗标题与说明也切到槽位语义，不再只是借用普通节点历史入口
- **槽位历史开始聚合同一槽位当前输出家族** — 输出历史查询现在会优先按当前槽位已知的输出家族聚合，而不是只按当前节点本身解释；弹窗里会明确显示“当前槽位输出 / 来源节点 / 定位槽位输出”
- **D3-03 继续从“能打开”推进到“能解释”** — 输出历史弹窗现在除了打开/恢复外，还会显示槽位级标题、说明与节点定位入口，开始把蓝图容器层的历史入口收口成更正式的槽位级历史视图

## [2.1.0-alpha.56] — 2026-04-19

- **槽位级错误归因开始精确到具体上游节点** — 蓝图输出槽位不再只显示“受上游失败影响”这类泛化提示；现在会在当前实例图里逆向查找最接近该输出槽位的失败节点，并直接把节点名、问题摘要和定位入口挂到槽位卡片上
- **等待产出状态也开始联动到上游运行节点** — 当蓝图仍在运行且某个输出槽位尚未产出时，槽位卡片会优先显示当前最相关的上游运行/推进节点，帮助用户理解“这个槽位到底在等谁”
- **D2-02 继续推进到“状态 + 归因 + 定位”联动** — 输出槽位现在已同时具备状态提示、关联节点卡片和一键定位，开始把槽位级错误提示从静态文案推进到可解释、可追踪的联动反馈

## [2.1.0-alpha.55] — 2026-04-19

- **蓝图输出历史不再只认 Markdown** — 输出历史现在会按当前输出节点类型筛选 `outputs/` 中的同类结果，并优先按同一输出家族聚合；图片 / 音频 / 视频输出也能进入历史弹窗，不再只有 `.md` 文本结果能看历史
- **历史恢复开始按真实输出类型回放** — 从输出历史恢复旧版本时，不再一律生成 `ai_output` 文本节点；现在会按历史文件真实类型恢复成文本 / 图片 / 音频 / 视频节点，图片也会继续保留 `display_mode=file`
- **旧画布开始在加载期强制收口“单槽位一个当前输出”** — 除了新运行时会主动降级旧绑定外，打开旧画布时也会对同一实例、同一输出槽位下残留的多个当前绑定做归一，只保留最新一个继续作为当前输出，进一步把 1A 从运行期扩到加载期

## [2.1.0-alpha.54] — 2026-04-19

- **单槽位单当前输出开始进入行为层** — 蓝图输出槽位再次产出新结果时，当前 fill 逻辑会主动把同一实例、同一槽位下历史残留的旧绑定节点降级为普通节点，不再继续保留多个“当前输出”身份
- **1A 口径不再只停留在 UI 过滤** — 之前只是容器 UI 默认显示最新一个输出；现在运行产生新结果时，也会同步清掉旧结果身上的 `blueprint_bound_*` 输出绑定元数据，让“一个当前输出，旧结果进历史/残留”开始真正落到数据层

## [2.1.0-alpha.53] — 2026-04-18

- **槽位级状态映射继续细化** — 输入槽位现在会明确区分“已绑定 / 缺少必填输入 / 可选未绑定”；输出槽位则会进一步尝试映射当前实例里的上游失败影响，不再简单把所有未产出都视为同一种待回填状态
- **输出槽位开始标记“受上游失败影响”** — 当实例内部某个失败节点能够沿当前运行图继续影响到某个输出槽位时，该槽位卡片会直接提示“受上游失败影响，本轮未能正常产出”，帮助用户把错误定位从节点层继续下沉到槽位层

## [2.1.0-alpha.52] — 2026-04-18

- **输出槽位开始显示更细的状态提示** — 蓝图容器里的输出槽位现在会直接区分“等待产出 / 运行失败，未产出 / 运行取消，未产出 / 已回填”，不再只有一个笼统的待回填状态
- **输出槽位补上历史入口** — 当当前输出绑定了文件时，槽位卡片里会额外出现“历史”按钮，可直接拉起该输出节点已有的历史版本弹窗，为后续正式蓝图输出历史收口先接好入口
- **输出历史弹窗正式接回 App 消息链路** — 之前 `OutputHistoryModal` 虽然存在，但没有真正挂进 App；现在已接上消息分发和弹窗挂载，可被蓝图输出槽位直接调用

## [2.1.0-alpha.51] — 2026-04-18

- **输出槽位现在可以直接定位真实输出** — 蓝图容器里的输出槽位不再只是显示“已回填 N”；当槽位已有当前输出时，槽位卡片里会直接显示当前输出标题，并提供“定位输出 / 打开文件”按钮，方便从容器直接跳到真实结果
- **蓝图容器内补了轻量错误面板** — 当实例运行中已有节点报错，或最近一次运行失败时，容器内部会直接出现轻量错误卡片，显示问题节点、错误类型、错误摘要和定位按钮，不必再只靠顶部工具栏或零散摘要排查
- **输出槽位开始按“一个当前输出 + 旧结果进历史”的口径收口** — 当前若检测到同一槽位残留多个绑定结果，容器会默认把最新一个视为当前输出并优先定位它，为后续正式输出历史铺路

## [2.1.0-alpha.50] — 2026-04-18

- **蓝图容器内补回显式总进度条** — 运行蓝图实例时，容器中间状态区现在会直接显示当前实例自己的总进度条和 `已完成 / 失败 / 跳过 / 运行中 / 等待中` 统计，不必再只盯顶部全局 PipelineToolbar
- **蓝图容器内新增独立取消按钮** — 当蓝图实例正在运行时，容器状态区会额外出现实例级取消入口，用户可以直接在容器内发起取消，不用把视线移到画布顶部工具栏
- **运行结束后容器仍保留最近一次进度统计** — 即使当前不在运行，蓝图容器里仍会保留最近一次运行的完成/失败/跳过计数，方便快速回看实例执行结果

## [2.1.0-alpha.49] — 2026-04-18

- **旧 `summaryGroups` 迁移改为并入正式 boards** — 不再只有在 `boards` 为空时才做迁移；现在旧文件里残留的 `summaryGroups` 会在加载时直接并入当前正式 `boards`，避免“部分旧分组信息因为文件里已经有 board 而被静默跳过”
- **旧 board/summaryGroup 双轨残留开始统一收口** — 若历史文件同时残留正式 `boards` 与早期 `summaryGroups`，现在会统一去重后保留在正式 `boards` 中，并清掉旧字段

## [2.1.0-alpha.48] — 2026-04-18

- **画布壳数据开始做加载期归一** — 打开旧画布时，`viewport` 会先做数值与范围校正，避免历史异常缩放值或非法坐标继续留在文件里
- **正式 boards 结构也开始顺手清洗** — `boards` 现在会在加载时去重、补默认名/颜色，并把过小或异常的 bounds 拉回到合理最小值
- **暂存区历史残留开始自动剔除** — 若 `stagingNodes` 里还残留了已经落到画布上的旧节点、与正式 board 冲突的条目、带蓝图残留元数据的脏节点，或重复/非法的 staging 项，现在会在加载时直接清掉并即时落盘

## [2.1.0-alpha.47] — 2026-04-18

- **旧版 board 伪节点开始在加载期迁移为正式 boards** — 历史画布里若还残留早期直接存进 `nodes` 数组的 `board` 结构，现在打开时会自动转成当前正式 `boards` 数据，不再继续以伪节点身份混在普通节点列表里
- **挂在旧 board 节点上的脏边会一并清掉** — 迁移 legacy board 时，会同步移除所有 source/target 指向这些旧 board 节点的历史边，避免旧结构继续污染当前画布

## [2.1.0-alpha.46] — 2026-04-18

- **多模态输入预算开始细化到图片级别** — 功能节点在真正发起视觉/图像相关请求前，会先按图片数量、总图片体积和保守估算的图片输入开销做一次统一裁剪，减少一次连太多图或超大图时直接被 provider 拒绝
- **预算裁剪不再只会照顾纯文本** — 旧逻辑只会裁剪文本长度，图片几乎不参与预算；现在图像编辑、图生视频以及支持图片输入的聊天链路都会先为图片预留预算，再对剩余文本做裁剪
- **节点会先提示本次输入被裁掉了什么** — 若本次运行因为预算限制省略了部分图片，或顺带裁掉了超长文本，节点状态区会先给出“保留几张图 / 省略几张图 / 裁掉几段文本”的提示，方便排查为什么本次输入和连线数量不完全一致

## [2.1.0-alpha.45] — 2026-04-18

- **旧节点组 bounds 现在会在加载期按真实成员重算** — 历史 `nodeGroups` 打开时，除了补 hub、清旧边和剔除失效成员引用，现在也会按当前仍存在的真实成员节点重新计算组框 bounds，减少“组内容已经变了，但旧组框尺寸还停留在历史状态”的错位
- **空节点组会在加载期直接清掉** — 如果旧文件里节点组成员已经被删空，但组配置本身还残留，现在会在打开时自动移除对应节点组与 hub 结构，并即时落盘

## [2.1.0-alpha.44] — 2026-04-18

- **蓝图占位节点与悬空连线的历史残留开始加载期清理** — 打开旧画布时，若残留了已经失去所属蓝图实例的输入/输出占位节点、仍挂着失效 `blueprint_bound_*` 元数据的真实节点，或 source/target 已不存在的悬空边，现在会在加载期自动清理并即时落盘，继续补“蓝图容器外历史元素迁移”的漏网场景

## [2.1.0-alpha.43] — 2026-04-18

- **节点组历史残留开始在加载期一并清理** — `nodeGroups` 迁移现在不再只会“缺什么补什么”；当旧画布里残留了失效的 `group_hub` 节点、指向旧 hub 的 `hub_member` 隐藏边，或节点组成员列表里还挂着已经不存在的节点时，加载期会同步清掉这些历史残留，减少“界面看起来更新了、磁盘里其实还留着旧组骨架”的情况
- **空节点组场景也会顺手收干净旧 hub 结构** — 若画布里已经没有任何 `nodeGroups`，但历史文件还残留 `group_hub` / `hub_member` 数据，现在打开时会自动清理并即时落盘

## [2.1.0-alpha.42] — 2026-04-18

- **旧蓝图实例开始做“严格迁移”清理** — 打开旧蓝图实例并拿到当前 definition 后，除了继续补齐缺失的输入/输出占位、内部功能节点和内部数据节点，现在也会反向清理历史残留的蓝图内部节点与重复/过期定义边，减少“看起来像迁移过了、其实旧骨架还留在里面”的半迁移状态
- **历史失效绑定会自动解绑而不是继续挂在实例上** — 若旧实例里残留了已经不再属于当前 definition 槽位的 `blueprint_bound_*` 绑定元数据，现在会在迁移时自动去掉，避免外部真实节点继续带着过期蓝图槽位身份

## [2.1.0-alpha.41] — 2026-04-18

- **修复打开设置即白屏的 React hook 顺序错误** — `SettingsPanel` 新增保存状态逻辑后，`useMemo` 放在了 `settings` 判空分支之后，导致“第一次设置快照还没到时”和“快照到达后”两次渲染的 hook 数量不一致，触发 React error #310；现已把相关状态准备前移，设置面板可正常打开

## [2.1.0-alpha.40] — 2026-04-18

- **设置页补回手动“保存设置”按钮** — 虽然设置本身仍会自动保存，但主设置面板和各个二级弹窗里现在都会额外显示一个明确的 `💾 保存设置` 按钮，方便用户手动确认
- **设置保存状态开始显式可见** — 设置区会显示 `已保存 / 待保存 / 保存中` 状态，并在收到宿主回传的最新设置快照后更新，减少“到底有没有真的记住”这种不确定感
- **设置按钮支持强制确认同步** — 点击“保存设置”会先冲刷仍在 debounce 队列里的输入，再主动向宿主请求最新设置快照，确保自动保存和手动确认两条路径都收口到同一份状态

## [2.1.0-alpha.39] — 2026-04-18

- **保存按钮现在会主动提醒用户还没真正写盘** — 顶栏「保存」按钮在 `未保存` 状态下会改为高亮并轻微脉冲，提醒当前画布仍有脏改动，避免用户只顾着继续编辑而忘记手动保存
- **保存中的反馈从纯文字扩展到按钮动效** — 当画布正在写盘时，保存按钮会切到更明显的进行中样式与呼吸动效，不再只有旁边那行小字在变化
- **保存失败时会直接把按钮切成错误态** — 若最近一次保存失败，保存按钮会进入醒目的错误高亮，用户可以更直观看到需要立即重试，而不是误以为已经落盘

## [2.1.0-alpha.38] — 2026-04-18

- **功能节点重复输入提示已去重** — 之前部分工具在“无输入”时会同时出现蓝色连接引导和黄色 warning，两条都在说“需连接输入”，造成重复提示；现在当 warning 已覆盖缺失输入时，不再额外显示同类蓝色提示
- **功能节点下拉已直接恢复为常规原生样式** — 不再继续折腾节点内/页面级浮层；功能节点中的服务商、模型和参数下拉现统一改回原生 `select`，避免再出现缩放变形、位置错位或边界联动问题
- **功能节点下拉改成“节点内浮层”模式** — 功能节点里的下拉不再使用页面级固定浮层，而是挂到节点本体里的独立覆盖层：既不会把节点高度撑长，也会随节点一起缩放/移动；菜单最大高度会被限制在节点底部以内，超出部分内部滚动
- **下拉浮层现在会跟随画布里的节点一起移动** — 之前为了解耦节点高度，把候选列表改成独立浮层后，只监听了窗口滚动/缩放，没覆盖 React Flow 画布的平移/缩放，导致菜单像“钉在页面上”；现在菜单打开时会持续跟踪锚点位置，随节点一起移动
- **常用模型过滤不再悄悄回退成全量列表** — 之前当常用模型配置为空，或常用模型 ID 没能在当前列表中命中时，功能节点还会兜底把全量模型重新展示出来；现在空配置会继续走默认常用模型逻辑，未命中时也不再直接回退全量列表
- **功能节点下拉菜单不再把节点边界一起撑长** — `SearchableSelect` 的候选列表已从节点内部普通布局改为独立浮层；列表继续保留固定最大高度，超出部分用滚动查看，但不再被功能节点自动高度同步纳入计算
- **功能节点默认常用模型提示改为首帧稳定显示** — 自动常用模型在模型列表尚未返回时，也会先按 provider 预置一组“乐观默认值”，减少重开画布后先无提示、随后再补出“仅显示常用模型”的二次变化
- **功能节点端口圆点裁切修复** — 之前为压住内容溢出，把整个节点外层设成了 `overflow: hidden`，连带把左右通道圆点裁掉半边；现在改为外层允许端口外露、内层表面单独裁切内容，圆点与命中区会完整显示
- **重开画布时开始真正恢复持久化视角** — 之前虽然 `.rsws` 已写入 `viewport`，但 React Flow 初次挂载时常先吃到默认视角，后续加载到真实画布数据时没有再次显式应用持久化视角，导致重开后看起来仍像回到初始页；现在在画布数据就绪后会主动把持久化视角重新打回画布实例
- **关闭/切走画布前会额外冲刷待保存视角状态** — 当画布还有待保存改动时，切标签、隐藏 Webview 或关闭前会尝试立即保存，降低“刚刚平移缩放过，但还没赶上 autosave 就关闭了”导致视角回退的概率
- **功能节点模型提示不再在重开画布后突然后跳** — 之前画布重开时，功能节点会先按“设置 / 模型列表尚未到位”的初态渲染，随后在常用模型与模型列表到达后再补出“仅显示常用模型”提示，导致节点高度二次变化；现改为预留稳定提示高度，减少重开画布后的视觉抖动
- **模型列表请求开始做前端去重并在打开画布后预取** — 画布拿到设置快照后，会对已配置可用的 provider 先做一次后台模型预取；功能节点和设置面板也改为复用统一的请求入口，避免同一 provider 因多个节点同时挂载而被重复请求
- **AIHubMix 的 LLM 默认常用模型再收口为 3 个** — 对 AIHubMix 自定义 provider，未手动配置常用模型时，文本功能节点现在优先只预置 `gemini-3.1-pro-preview`、`gpt-5.4`、`claude-sonnet-4-6`
- **默认热门常用模型继续收口到 6 个文本模型** — 未手动配置常用模型时，内置热门候选现在收口为 GPT / Gemini / Claude 各 2 个；Qwen、DeepSeek 等不再默认塞进文本功能节点下拉
- **热门常用模型匹配逻辑改为按模型家族自动识别** — 不再只靠写死精确 ID；现会优先识别 GPT-5.4 / GPT-5.4 Mini、Gemini 3.1 Pro / Gemini 3 Flash、Claude Sonnet 4.6 / Claude Opus 4.6，并在新 ID、preview 后缀或旧版 fallback 场景下尽量自动命中
- **Anthropic 默认模型更新到 Claude Sonnet 4.6** — 内置 Anthropic provider 的默认模型与静态回退列表已同步到 Claude 4.6 代际；同时补入 4.6 家族的能力提示，避免默认模型仍停留在 4.5
- **AIHubMix 原始流输出保留修复** — 修复 Claude 等经 AIHubMix 走自定义 OpenAI 兼容 provider 时出现“接口成功但输出文件为空”的问题；除标准 `delta.content` 外，现开始兼容 `reasoning_content`、`reasoning_details` 与 `tool_calls`
- **未知流式格式改为优先保留原始 SSE** — 当插件无法识别标准正文输出时，不再静默写出空文件，而是把 reasoning / tool calls / raw SSE chunks 全量保留进输出文件，避免额度浪费，也方便用户反馈真实返回格式
- **AIHubMix 官方 max_output 现在会自动顶满** — 对走 AIHubMix 的自定义 OpenAI 兼容 provider，插件会先查询官方 `Models API` 中该模型的 `max_output`，并把 `max_tokens` 自动设为模型允许的最高值；这也覆盖了 Claude 普通版 / `-think` 变体这类需要显式放开输出上限的场景
- **AIHubMix 上下文窗口改为按官方 `context_length` 自适应** — 聊天类功能节点现在会基于官方模型元数据动态放宽 PDF / Office / 文本提取上限，并按估算 token 预算裁剪输入，不再固定卡死在 `100_000` / `200_000` 字级别
- **新增全局“最大输出 / 最大上下文”设置** — 设置面板与 VSCode 配置现在新增 `researchSpace.ai.maxOutputTokens` / `researchSpace.ai.maxContextTokens`；默认 `0` 表示自动取最大，若用户填写了更小的上限，则按 `min(模型最大值, 设置值)` 生效
- **Provider 预算逻辑扩展到全通道** — 统一预算逻辑已从 AIHubMix 扩展到所有聊天类 provider：Anthropic 会按模型家族启发式给出已知上限，Ollama 会读取本地 `/api/show` 的 `num_ctx` / `model_info`，Copilot 与未知 custom provider 则至少会使用用户配置的上限做输入裁剪，并尝试把输出上限透传给 provider
- **新增“常用模型”管理** — 设置面板现在可为每个 provider 勾选常用模型；设置里的模型管理仍显示完整模型列表，但功能节点默认只显示常用模型，降低超长模型列表的操作负担
- **设置面板拆成主入口 + 二级弹窗** — 原本越滚越长的设置页已改为简洁主面板，详细 provider / 多模态 / 画布 / 宠物配置进入二级弹窗编辑；常用模型也有独立弹窗管理
- **常用模型现支持排序** — 常用模型弹窗中现在可以拖拽排序，也可用上下按钮微调顺序；功能节点中的模型下拉会按该顺序展示
- **文本服务商二级设置再细分为标签页** — LLM 设置弹窗进一步拆成“概览 / 内置服务商 / 自定义服务商”，减少继续堆长列表带来的查找成本
- **画布重新打开时开始恢复上次视角** — 现在会在平移/缩放结束后持久化 `viewport`，再次打开同一画布时优先恢复到上次关闭前的位置；只有没有有效历史视角时才回退到初始 `fitView`
- **函数节点高度同步继续收口** — 针对功能节点在改其它内容后容易出现“运行按钮上方溢出”或“按钮下方留白很多”的问题，调整了最小高度、改进测量方式为更稳定的双帧/真实渲染高度测量，并补充更多触发高度重算的依赖，减少边界漂移
- **默认常用模型开始预置一组热门候选** — 当某个 provider 还没有手动配置常用模型时，插件会按内置 provider / AIHubMix / Ollama 的当前可用模型给出一组默认常用候选，功能节点不再轻易退回显示所有模型
- **旧功能节点自动跟随最新工具定义** — 打开旧画布或收到最新 `toolDefs` 时，历史功能节点现在会自动刷新 `input_schema` 并补齐新增参数默认值；`FunctionNode` 参数渲染也改为优先使用当前工具定义，减少“老节点没更新、新节点正常”的长期不一致问题
- **画布加载迁移改为即时落盘** — 旧画布在打开时若触发节点组 hub 补齐、蓝图尺寸归一化、`summaryGroups → boards`、功能节点 schema 补齐等加载期迁移，现在会立即自动保存，避免“本次会话看起来正常、但磁盘文件其实还没真正升级”的半迁移状态
- **旧蓝图实例开始补齐当前定义** — 蓝图索引刷新后，Webview 现会向宿主补拉对应蓝图定义，并尝试为旧实例补齐缺失的输入/输出占位、内部功能节点、内部数据节点与定义边；同时为实例内部节点补写 `blueprint_source_*` 溯源元数据，给后续更稳定的实例迁移打基础

## [2.1.0-alpha.37] — 2026-04-18

- **蓝图取消语义开始收口** — PipelineToolbar 在取消后不再立刻消失，而是显示“取消中 / 已取消”；蓝图容器会持久化最近一次取消状态
- **最近问题节点可定位** — 蓝图容器开始记录最近问题节点，并支持直接从容器中定位到失败/中断节点
- **新增 v2.0 主线总控待办** — 仓库根目录新增 `待办清单.md`，统一维护 v2.0 主线、蓝图子线与阶段计划完成情况

## [2.1.0-alpha.36] — 2026-04-18

- **ESLint 9 flat config 补齐** — 新增根目录 `eslint.config.cjs`，恢复 `npm run lint` 可执行
- **清理无效 lint 注释** — 移除历史遗留的无效 `eslint-disable` 注释，减少 lint 噪音，便于继续推进蓝图主线改造

## [2.1.0-alpha.34] — 2026-04-17

- **AI 工具警告文案统一** — 节点内黄色 warning banner 统一改为更短、更一致的“需…”语气，减少不同工具之间提示风格跳变
- **README / 规划清单同步到当前实现** — 文档已补充 Doubao 图像生成/编辑、多图融合、组图输出、节点内警告提示、多模态输出模型标签，以及新增的 AIHubMix 默认模型配置项

## [2.1.0-alpha.33] — 2026-04-17

- **AI 工具警告提示统一覆盖** — FunctionNode 现按统一规则为各类 AI 工具给出前置警告：文本工具缺少必需输入、Chat 缺少 Prompt、RAG 缺少问题、图像生成缺少描述、图像编辑缺少图像或提示词、多图融合缺少图像或指令、视频生成缺少描述、TTS/STT 缺少正确输入类型等，尽量把问题前置到节点内提示，而不是等运行时报错

## [2.1.0-alpha.32] — 2026-04-17

- **多模态输出节点补齐模型标签** — 图像 / 音频 / 视频 / 转录等经 AIHubMix 生成的输出节点现在都会写入 `ai_provider / ai_model` 元数据，并在节点头部显示与文本类输出一致的服务商 / 模型 badge
- **组图输出补齐前置输入警告** — 组图输出节点在既没有连接文本节点、也没有填写组图描述参数时，会直接在节点内给出缺失警告，减少“点运行才报错”的无效尝试

## [2.1.0-alpha.31] — 2026-04-17

- **图像类参数面板按模型能力动态收口** — 图像生成 / 图像编辑节点现在会根据当前实际生效的模型自动隐藏不适用参数：Gemini 不再显示 Doubao 专属的 `size / watermark / web_search`，Doubao 也不再显示 Gemini 专属的 `aspect_ratio`
- **图像生成提示改为强制文本节点输入语义** — 对普通图像生成节点，界面提示改为明确要求连接文本节点提供描述，只把下方参数保留给模型、风格和输出控制，减少把参数区误当主提示词输入区的歧义

## [2.1.0-alpha.30] — 2026-04-17

- **图像编辑改回“图像 + 文本节点”输入模式** — 移除图像编辑节点内部的“编辑指令”参数，运行时改为读取连接进来的文本节点内容作为编辑提示词；节点也会在只连了图像、没连文本时给出明确警告
- **Doubao 图像尺寸参数改为合法格式** — Doubao 图像生成 / 图像编辑 / 多图融合 / 组图输出统一改用接口实际接受的 `2k / 3k / 宽x高` 格式；对旧画布中残留的 `1K / 2K` 也会在运行时自动归一化，避免再触发 `size is not valid` 的 400

## [2.1.0-alpha.29] — 2026-04-17

- **图像生成 / 图像编辑补入 Doubao Seedream 5.0 Lite** — AIHubMix 多模态设置与节点模型下拉中新增 `doubao-seedream-5.0-lite`；现有图像生成 / 图像编辑节点会按 Doubao `predictions` 接口直接走文生图 / 图生图
- **新增多图融合与组图输出工具** — 画布里新增「多图融合」与「组图输出」两个 AI 工具，并补充各自独立默认模型设置；多图融合要求至少连接 2 张图像，组图输出支持一次回填多张连贯图
- **FunctionNode 高度同步改为内容尺寸驱动** — 节点高度不再只依赖少量状态切换触发重算，而是由 `ResizeObserver` 直接驱动内容区测量与外框同步，修复聊天 / 多模态参数区再次出现的边界框溢出回归

## [2.1.0-alpha.28] — 2026-04-17

- **暂存架删除不再误删工作区文件** — 从文件夹拖入暂存架的外部文件节点，现在点击 `✕` 只会从暂存架移除，不会再删除工作区里的真实文件
- **新建笔记 / 实验 / 任务改为“落画布时落盘”** — 这三类节点在暂存架阶段只保留草稿标题和初始内容，不会提前创建实体 `.md` 文件；只有真正拖到画布后才会在 `notes/` 下生成文件
- **暂存架草稿物化链路补齐** — Webview 在放置草稿节点时会请求宿主创建文件，创建成功后再把节点正式提交到画布；物化过程中暂存架条目会显示“创建中…”，避免重复点击或半状态删除

## [2.1.0-alpha.27] — 2026-04-17

- **蓝图实例运行入口接通首轮闭环** — 正式蓝图容器上的 `▶ 运行` 按钮已可用；运行前会先校验必填输入，缺失时直接阻止执行并给出明确报错
- **蓝图执行计划改为实例内闭包** — 蓝图运行不再沿整张画布盲目扩散，而是只对当前实例内部功能节点构建拓扑计划；多头蓝图也会在同一轮内按层执行
- **中间产物开始就地更新** — 实例内部功能节点如果本来指向蓝图内部中间数据节点，运行结果会优先替换该内部节点并重绑后续边，而不是继续在容器外额外堆一个输出卡片
- **实例外 Pipeline 上游显式拦截** — 当前首轮闭环仍不支持“实例外功能节点 → 实例内功能节点”的 Pipeline 依赖；运行前会显式阻止，避免半执行状态混入主线
- **执行控制继续复用现有 Pipeline 工具栏** — 蓝图实例运行仍走统一的 Pipeline 进度、暂停、继续、取消链路，避免为蓝图另起一套执行状态机

## [2.1.0-alpha.22] — 2026-04-17

- **通道从箭头改回圆点** — 取消节点边缘箭头通道，恢复为更克制的圆点句柄，避免通道本身过度抢视觉
- **圆点接近反馈增强** — 通道保留更大的不可见命中区，鼠标靠近时圆点会高亮并做呼吸式闪烁，更容易发现可连线入口
- **悬停提示输入 / 输出语义** — 通道悬停时会弹出“输入通道 / 输出通道”提示，帮助用户在不依赖箭头形状的情况下理解当前句柄职责

## [2.1.0-alpha.21] — 2026-04-17

- **通道箭头改为更醒目的头部比例** — 在统一向右的小箭头基础上，进一步放大箭头头部占比，并增强边缘对比与 hover 光感，让通道在节点边缘更容易被一眼识别

## [2.1.0-alpha.20] — 2026-04-17

- **通道箭头继续瘦身** — 进一步缩短并收窄箭头尾部，同时把箭头头部做得更尖，让通道更接近流程图里的“小箭头”观感

## [2.1.0-alpha.19] — 2026-04-17

- **通道箭头方向统一为向右** — 按当前画布工作流“从左向右推进”的视觉语义，左右两侧通道现都统一使用向右箭头，避免输入侧再出现反向箭头打断阅读方向
- **箭头尾部进一步收窄缩短** — 继续回修上一版箭头仍显得偏厚、偏长的问题，让通道更接近简洁的流程箭头而不是粗块状标记

## [2.1.0-alpha.18] — 2026-04-17

- **箭头通道视觉改为更明确的实体箭头** — 修正上一版通道看起来更像矩形块的问题，左右通道现直接使用更明显的左箭头 / 右箭头轮廓，方向感更强
- **右键菜单关闭判定改为“菜单内点击不抢先关闭”** — 修复菜单关闭监听过于激进，导致在右键菜单里点击条目时先被全局关闭逻辑打断的问题；现在会先执行菜单项，再按既有动作自然关闭
- **保留外部点击 / 继续右键 / 画布滚动自动收口** — 菜单内部点击不会误杀，但点击外部、对其他元素再次右键、或滚动画布时仍会自动关闭，维持单菜单收口行为

## [2.1.0-alpha.17] — 2026-04-17

- **节点通道改为箭头语义** — 数据节点、功能节点、节点组与蓝图实例的左右通道统一改成箭头形态，直接提示“左侧输入 / 右侧输出”的方向约束，降低误把输出接回输入侧导致流程失败的低级操作成本
- **连接线选中反馈显著增强** — 普通数据流连线与 Pipeline 连线在左键或右键选中后都会显示更强的高亮描边与光晕，方便用户明确当前真正选中了哪一条边
- **连接线补齐右键删除** — 现在右键普通连线或 Pipeline 连线会先选中并高亮该线，再弹出删除菜单，不需要只靠键盘 Delete 才能移除边
- **上下文菜单改为跟随画布元素移动** — 节点、蓝图、节点组与画板的右键菜单不再固定在屏幕视口，而是锚定到画布元素本身；平移画布时菜单会跟随元素一起移动
- **同一时刻只保留一个右键菜单** — 新的上下文菜单关闭机制会在继续点击、右键其他元素、滚动画布或开始拖拽时自动收口旧菜单，避免多个菜单同时残留
- **右键即选中并高亮目标元素** — 节点、节点组、蓝图、画板和连接线在右键打开菜单前都会先进入高亮选中状态，减少“菜单开了但不知道当前操作对象是谁”的交互歧义

## [2.1.0-alpha.16] — 2026-04-17

- **蓝图实例化开始按完整定义恢复真实结构** — 从蓝图库点击或拖拽蓝图到画布时，宿主现在会先读取完整 `*.blueprint.json`，再在画布中恢复功能节点、内部数据节点、输入占位节点、输出占位节点与内部边，而不再只靠索引摘要生成单个蓝图卡片
- **蓝图实例外层改为边界框包裹真实节点** — 首轮结构化实例会自动创建一个带蓝图标题的外部边界框，把恢复出来的节点整体包住，朝“蓝图就是可复用 Pipeline 模板”而不是摘要容器继续推进
- **输入 / 输出占位节点获得最小可见样式** — 占位节点现会带蓝图颜色、虚线边框和“输入占位 / 输出占位”标识，方便区分真实节点与占位节点

## [2.1.0-alpha.15] — 2026-04-17

- **蓝图定义开始保留原始 pipeline 结构信息** — 蓝图 JSON 现在新增内部数据节点定义与更明确的占位语义，不再只保存输入/输出数量与功能节点摘要，为后续“实例化时恢复原始结构”铺路
- **输入 / 输出占位语义进入蓝图草稿层** — 输入槽位和最终输出槽位现在会记录占位样式、绑定提示与替换策略，创建阶段就开始服务后续“拖入即替换 / 连线即绑定”的实例交互目标
- **创建对话框开始展示保留节点与占位意图** — 蓝图创建弹窗现在会额外显示保留的内部数据节点，并明确提示哪些对象未来会作为占位框出现，避免继续把蓝图理解成摘要容器

## [2.1.0-alpha.14] — 2026-04-17

- **蓝图实例首帧尺寸也会按新规范归一化** — `initCanvas()` 现会先按蓝图节点自身保存的输入/输出统计字段修正旧实例尺寸，不再依赖 `blueprintIndex` 回来后才把旧卡片拉大
- **蓝图重载修复继续前置到初始化阶段** — 即使蓝图库索引因为时序或刷新延迟稍晚到达，旧蓝图实例在画布首轮加载时也不会先以过小边界渲染一版再跳变
- **阶段定位仍然保持在 Phase C** — 这一轮仍然是蓝图实例加载一致性修复，未开始 Phase D 的执行编排实现

## [2.1.0-alpha.13] — 2026-04-17

- **旧蓝图实例在重载时会自动迁移到新容器结构** — 画布重新打开并收到 `blueprintIndex` 后，现有蓝图节点会按最新蓝图定义回填输入/输出槽位、统计信息和容器尺寸，不再只让新创建实例使用新布局
- **蓝图实例尺寸与槽位元数据统一收口** — 新建实例和重载迁移改为复用同一套蓝图元数据构造逻辑，避免出现“摘要计数已更新但输出槽位列表仍是旧的”这种半同步状态
- **阶段定位仍然保持在 Phase C** — 这一轮仍然是在修复蓝图实例加载与显示一致性，还没有进入 Phase D 的执行编排升级

## [2.1.0-alpha.12] — 2026-04-17

- **蓝图实例补齐输出槽位展示** — 实例容器右侧现会显示蓝图定义中的输出槽位，并同步渲染对应的右侧输出通道，为后续 Phase D 的执行回填预留统一容器语义
- **实例级输入校验态进入容器本身** — 蓝图实例中间栏新增输入校验状态，会直接显示“可运行”或“缺少 N 个必填输入”，把实例是否满足运行前提明确挂回容器
- **蓝图实例布局升级为三栏结构** — 容器改为“输入槽位 | 实例状态/摘要 | 输出槽位”三栏布局，尺寸计算也会同时考虑输入侧和输出侧内容，不再只按输入槽位估算
- **阶段定位继续保持在 Phase C** — 这一轮仍然只是在补蓝图实例的加载与可见状态，不涉及执行计划和蓝图运行闭环

## [2.1.0-alpha.11] — 2026-04-17

- **蓝图实例边界改为按内容自适应** — 蓝图实例化时不再沿用过小的固定尺寸，容器宽高现在会按输入槽位数量和摘要区布局计算，避免首轮实例卡片一加载就出现内容溢出
- **实例摘要改为双列栅格** — 蓝图卡片右侧的输入 / 中间 / 输出 / 功能节点统计改成 2×2 布局，减少纵向堆叠导致的高度压力
- **蓝图文件路径区域回收到底部** — 路径显示仍保留，但会在更宽的容器里自然换行，减少实例加载后第一屏就被路径文本挤爆的情况

## [2.1.0-alpha.10] — 2026-04-17

- **蓝图实例输入槽位接入真实连线** — 蓝图容器节点不再只是静态摘要卡片，现会把蓝图定义中的输入槽位渲染为真实 `Handle`；数据节点可直接像连接功能节点一样连接到蓝图实例输入槽位
- **槽位绑定状态回收到实例容器** — 蓝图实例会在卡片上显示每个输入槽位当前已绑定数量、必填状态与单/多输入约束，实例级状态不再需要另存一套绑定表
- **蓝图输入绑定继续复用现有边模型** — 绑定关系仍然落在现有 `data_flow` 边上，通过 `targetHandle/role` 指向蓝图输入槽位，避免为了蓝图实例再分叉一套专用连接语义
- **蓝图槽位标签进入边展示链路** — 连到蓝图实例的边会沿用现有边标签显示逻辑，并优先显示蓝图输入槽位标题，而不是只露出内部 slot id

## [2.1.0-alpha.9] — 2026-04-17

- **蓝图库最小入口接入左侧工具面板** — `AiToolsPanel` 现会显示当前工作区蓝图库索引，支持查看、拖拽和点击 `+` 直接实例化蓝图
- **蓝图实例容器节点首轮落地** — 新增 `blueprint` 节点类型与 `BlueprintContainerNode`，蓝图现在可以作为独立实例对象回到画布，而不是停留在文件层
- **实例化继续复用现有节点引擎语义** — 蓝图实例目前以单节点容器卡片呈现，保持与现有画布节点相同的拖拽、选中、持久化链路，不另起一套 overlay 交互层
- **蓝图库索引补齐结构摘要** — 注册表条目现在包含输入槽位、中间槽位、输出槽位和功能节点数量，实例容器可直接展示蓝图定义的基本规模

## [2.1.0-alpha.8] — 2026-04-17

- **蓝图创建对话框进入可编辑阶段** — `CreateBlueprintDialog` 不再只是只读预览，现可修改蓝图名称、颜色、描述以及输入/中间/输出槽位的名称、必填状态和多输入开关
- **蓝图落盘链路接通** — 新增 `blueprint-registry.ts`，蓝图现可保存到工作区 `blueprints/` 目录下的 `*.blueprint.json` 文件，并在重名时明确阻止覆盖
- **最小蓝图库索引回传** — Extension Host 在画布打开、`ready` 和蓝图文件变化时都会回传 `blueprintIndex`，为后续蓝图库面板和实例化入口提供基础数据
- **保存后自动收口当前草稿** — 蓝图保存成功后，当前创建对话框会自动关闭并刷新索引；蓝图创建流程从“预览草稿”推进到“草稿 → 保存文件”闭环

## [2.1.0-alpha.7] — 2026-04-17

- **蓝图草稿定义首轮落地** — 新增 `src/blueprint/blueprint-types.ts` 与 `blueprint-builder.ts`，冻结首版蓝图草稿结构：输入槽位 / 中间槽位 / 输出槽位 / 功能节点 / 边 / 元数据不再靠 UI 临时猜测
- **多选工具栏接入“创建蓝图”入口** — 选中包含功能节点的一段工作流后，可直接从 SelectionToolbar 触发 `createBlueprintDraft`
- **蓝图草稿宿主链路接通** — Extension Host 新增 `createBlueprintDraft` 消息处理，按当前选区与当前画布快照生成结构化蓝图草稿并回传 Webview
- **蓝图草稿预览对话框** — Webview 首轮接入蓝图草稿预览弹窗，可直接查看识别出的输入槽位、中间/输出槽位、功能节点与边结构
- **首版保持严格选区模式** — 当前若选中的功能节点仍依赖未选中的上游，或选区里直接包含 `group_hub`，会明确报错而不是静默猜测，避免把隐藏依赖带进蓝图定义

## [2.1.0-alpha.6] — 2026-04-17

- **文档提取跨平台 fallback 收口** — OOXML / OpenDocument / EPUB 的文本提取改为优先走内置 zip 解析，不再依赖系统 `unzip`；`rtf` 增加纯文本 fallback；`doc/dot` 在非 macOS 或 `textutil` 不可用时回退到 legacy 文本抽取；`xls/ppt/xlt/pps/pot` 的 best-effort 文本提取改为内置实现，不再依赖系统 `strings`

## [2.1.0-alpha.5] — 2026-04-17

- **Office 模板 / 放映变体继续补齐** — 新增 `dot` / `dotx` / `dotm`、`xlt` / `xltx` / `xltm`、`pps` / `ppsx` / `ppsm`、`pot` / `potx` / `potm` 导入支持；对应 Note / Data 节点映射、Explorer 右键入口、文件监听和 fallback 识别已同步扩展
- **Flat OpenDocument 首轮接入** — 新增 `fodt` / `fods` / `fodp` 支持：直接读取 XML 文本，不再只覆盖 zip 版 `odt/ods/odp`
- **提取链路继续复用同类实现** — `dotx/dotm` 复用 Word OOXML 提取，`ppsx/ppsm/potx/potm` 复用 PowerPoint OOXML 提取，`xltx/xltm` 复用 Excel OOXML 提取；`dot` 走 `textutil`，`xlt/pps/pot` 继续走 legacy best-effort 文本抽取

## [2.1.0-alpha.4] — 2026-04-17

- **老 Office 格式首轮接入** — 新增 `doc` / `xls` / `ppt` 导入支持，不再只覆盖新 Office；其中 `doc` 归入 Note，`xls` 归入 Data，`ppt` 归入 Note
- **第二批文档格式补齐** — 继续补入 `docm` / `xlsm` / `pptm`、`rtf`、`odt` / `ods` / `odp`、`epub`；对应导入入口、文件监听和 fallback 映射已同步扩展
- **提取策略分层** — `doc/rtf` 优先走 macOS `textutil`，`docx/xlsx/pptx/docm/xlsm/pptm` 走 OOXML 解包，`odt/ods/odp/epub` 走 zip/XML/HTML 首轮提取，`xls/ppt` 走 best-effort 文本提取；当前目标是“可导入、可预览、可喂给 AI”，不是版式保真

## [2.1.0-alpha.3] — 2026-04-17

- **Office 文件首轮接入** — 新增 `docx` / `pptx` / `xlsx` 导入支持：Word 与 PowerPoint 会进入 Note 节点，Excel 会进入 Data 节点，不再只能卡在资源管理器里导不进画布
- **OOXML 文本提取链路补齐** — Extension Host 对 `docx` / `pptx` / `xlsx` 增加首轮解包提取：`docx` 读取正文/页眉页脚文本，`pptx` 提取各页 slide 文本，`xlsx` 提取工作表单元格内容并转成制表文本，支持卡片预览、全文请求与 AI 输入
- **说明与限制显式化** — 这一轮先支持现代 OOXML（`docx/xlsx/pptx`），暂不覆盖旧二进制 `doc/xls/ppt`；Office 提取目前属于“基础可读文本”级别，不等同于完整版式保真解析

## [2.1.0-alpha.2] — 2026-04-17

- **主流文件导入范围扩充** — 新增一批更常见的文本/代码/媒体格式映射：`markdown` / `mdown` / `mkd` / `rst` / `adoc`，`mjs` / `cjs` / `mts` / `cts` / `h` / `hpp` / `php` / `lua` / `ps1` / `vue` / `svelte` / `astro` / `ipynb` / `jsonl` / `ndjson` 等现在都可直接加入画布
- **图片/音视频格式补齐** — Image 节点补充 `svg` / `bmp` / `avif` / `ico`，Audio 节点补充 `ogg` / `oga`，Video 节点补充 `m4v`，减少“常见文件拖不进来”的割裂感
- **导入入口与文件监听同步放宽** — Explorer「Add to Canvas」菜单匹配范围、宿主文件监听 glob、硬编码后备映射与语言识别表已同步更新，避免“能识别但右键没有入口”或“导入后文件变化不刷新”的半支持状态

## [2.1.0-alpha.1] — 2026-04-17

- **收口版执行计划统一** — 单节点运行、批量运行与 Pipeline 运行进一步共享同一套 `execution-plan` 语义，Hub 展开、输入顺序、数据流 / 管道流依赖和下游注入不再各走各路
- **执行错误可见性补齐** — 功能节点与 Pipeline 工具栏现在统一展示 `missing_input` / `missing_config` / `run_failed` / `skipped` 等结构化问题，减少“节点没跑但看不出来为什么”的假状态
- **节点容器视觉统一** — 数据节点、功能节点、节点组统一了边框粗细、hover/selected 反馈、标题字号与端口外观，节点组更明确呈现为收口容器而不是另一张文件卡片
- **首屏加载观察面建立** — 大画布加载期间新增节点数、媒体请求数、全文请求数、组边界重算次数、首轮渲染时间等观察信息，后续性能问题不再只能靠主观卡顿感猜
- **高频更新源第一轮收口** — 搜索输入增加 debounce，媒体 URI / 节点预览改为按帧批量回填，相同内容不再重复写入 store，降低大画布和流式更新时的无效重渲染
- **加载提示关闭条件更真实** — 首屏 loading notice 现在会等待首轮渲染就绪、媒体恢复和关键全文补载完成，而不再只因为初始 JSON 到达就过早消失

## [2.0.0-alpha.14] — 2026-04-16

- **版本与发布资料统一到 alpha.14** — `package.json`、README、CHANGELOG 与 GitHub Release Note 全部同步到 `v2.0.0-alpha.14`，避免安装包、仓库说明与发布页文案出现版本错位
- **节点卡片全文态改为尺寸驱动按需补载** — 文本类节点不再只能通过“预览”弹窗拿到全文；当节点首次被拉伸到足够尺寸时，会自动请求全文内容，减少“信息像丢了”的交互错觉
- **节点展示态写回画布数据** — 新增卡片内容展示模式元数据，重新打开画布时可恢复节点上次关闭前的摘要态 / 全文态，而不是统一退回固定摘要
- **全文缓存与摘要态切换回归一致** — 修复节点已经加载过全文后，缩回摘要态仍继续显示全文缓存的问题；卡片正文现在严格跟随节点自己的展示模式
- **继续保持 alpha 预览警告** — 发布说明继续明确标注该版本稳定性弱于 `v1.2.2`，仅建议在备份后的副本工作区中试用

## [2.0.0-alpha.13] — 2026-04-16

- **发布定位明确为开发预览版** — 这次 GitHub 同步面向 `2.0` 新链路试用，不作为稳定版替代；README 与发布说明都会明确提醒“稳定性弱，慎重使用，建议先备份工作区”
- **AI 流式输出改为分帧缓冲** — Webview 侧对 `aiChunk` 更新增加按帧合并，避免多节点 Pipeline 连续运行时因流式文本过密触发 React 嵌套更新风暴，缓解“第一个节点完成、第二个节点启动即崩溃”的问题
- **文件节点刷新与画布保存解耦** — 新增 `canvasStateSync` 轻量消息，画布当前内存态会实时同步到 Extension Host，而不必等待手动保存或 3 分钟自动保存；新创建节点在文件保存后即可命中文件监听并刷新卡片信息
- **文件监听优先使用活动画布内存态** — 文件变更、删除、重命名时，监听器优先读取当前打开画布的 `document.data`，不再只依赖磁盘上的旧 `.rsws` 快照，避免“节点已在画布中，但扩展侧还不知道它存在”的状态错位
- **节点元数据回写链路收口** — 文件监听刷新 `content_preview`、`ai_readable_chars`、缺失状态等元数据后，会同步更新活动文档内存态并写回 `.rsws`，减少“刷新了 UI 但下次保存又被旧数据覆盖”的问题

## [2.0.0-alpha.12] — 2026-04-15

- **节点组引擎回归原生节点** — 废弃 `overlay + 外挂 port 节点` 的试错方案，节点组改回原生 `NodeGroupNode` 渲染，组框、标题栏、折叠态和左右双通道统一挂在同一个 React Flow 节点实体上
- **节点组语义改为 hub 收口** — 节点组不再在连线时临时展开为多条外部边，而是持久化一个真实 `group_hub` 节点，并用隐藏的 `hub_member` 边把成员节点汇聚到 hub；对外连线始终只有一条显式边，执行时再展开为成员输入
- **节点组主体改为指针穿透** — 节点组主体区域不再拦截组内文件节点的鼠标交互，只让标题栏、菜单与左右端口接管交互，避免再次出现“组能显示但把成员都挡住”的回归
- **节点组双通道补齐** — 节点组恢复与普通节点一致的左入右出双通道，右侧用于整组批量输出，左侧用于整组批量接入，为后续“爆炸/蓝图”能力保留一致心智模型
- **节点组拖拽命中区修正** — 标题栏整体都作为整组拖拽句柄，底层复用原生节点拖拽链路，再由 store 把 hub 位移翻译成成员位移，避免出现“点中标题栏却拖不动”或“只移动框不移动成员”的交互错觉
- **节点组通道交互显式化** — 左右通道都由原生句柄本体负责命中与连线，并继续跟随组框颜色，避免再出现“看起来像通道、实际只是装饰”的问题

## [2.0.0-alpha.11] — 2026-04-15

- **节点组通道可见性回修** — `GroupPortNode` 改回“真实句柄自身就是可见圆点”的实现，避免组通道只剩偏移 `Handle` 导致在组框边缘看起来像消失
- **节点组通道定位微调** — 展开态与折叠态的组通道锚点重新对齐到组框右侧，保持“看得见即可拖拽连线”的直觉

## [2.0.0-alpha.10] — 2026-04-15

- **保存脏状态去伪存真** — 自动保存只会由真正改变 `.rsws` 内容的操作触发；选中节点、ReactFlow 内部尺寸/选择回报等瞬时 UI 变化不再反复把顶栏状态打回“未保存”
- **保存提示收口** — 顶栏保存状态改为纯文字提醒，不再带底色和边框；保存按钮改成与其他按钮一致的普通样式与单色图标
- **文本节点可读元数据统一** — `ai_output`、`note` 等文本节点的 `ai_readable_chars` 现在由统一链路维护：AI 生成输出时写入全文长度，文件变更监听也会同步刷新预览和全文长度
- **AI 输出提示不再假装全文 300 字** — 当只有预览、没有全文长度元数据时，底部提示会明确显示“已载入预览”，避免把 300 字预览误报成全文长度

## [2.0.0-alpha.9] — 2026-04-15

- **保存状态可见化** — 顶栏新增画布保存状态提示，明确显示 `已保存` / `未保存` / `几秒后自动保存` / `保存中` / `保存失败`
- **手动保存入口** — 顶栏新增 `💾 保存` 按钮，并支持 `Ctrl/Cmd+S`，与自动保存共用同一条宿主写盘链路
- **保存回执补齐** — Extension Host 在自动保存和手动保存完成后主动回传 `canvasSaveStatus`，前端不再依赖“猜测是否已经写盘成功”

## [2.0.0-alpha.8] — 2026-04-15

- **Pipeline 入口条件收紧** — `▶▶ Pipeline` 只在多选/选区工具栏中出现，不再因单独选中一个节点而显示，保持“外部入口”语义并与蓝图入口一致
- **文件节点底部信息固定** — 数据节点改为“头部固定 + 正文滚动 + 底部元信息固定”，`AiReadabilityBadge` 与文件路径/模型标签不再在缩高时被压扁
- **节点组通道去装饰化** — 移除组框上的假圆点，仅保留真实的 `GroupPortNode` 句柄；连接校验同步支持节点组 → 功能节点，恢复可拖拽连线
- **执行前画布强制同步** — `runFunction` / `runBatchFunction` / `runPipeline` 现在携带当前画布快照并在宿主侧立即写回，修复“删除旧功能节点后仍触发旧节点”与 `找不到功能节点或 ai_tool 配置` 的时序问题

## [2.0.0-alpha.7] — 2026-04-15

### Fixed
- **Pipeline 入口回归** — 移除 FunctionNode 内部的 `▶▶ Pipeline` 按钮，恢复为外部入口，避免多节点场景下入口语义混乱，并保持与后续蓝图设计兼容
- **数据节点缩放布局** — 调整 DataNode 结构，标题栏与预览按钮改为固定头部区域，仅正文滚动，节点调节高度时不再把头部一起压缩
- **节点组通道恢复** — 恢复节点组右侧通道的可见提示与可连线行为，组框与折叠卡片重新显示独立圆点，通道节点层级上移避免被外框覆盖
- **画布高 CPU 回归** — 优化 `Canvas` 的 `displayNodes` 构造逻辑，非搜索/非折叠场景下复用原始节点对象，减少打开画布后因内容更新导致的整画布重建与高频重渲染

---

## [2.0.0-alpha.6] — 2026-04-15

### Fixed
- **Pipeline 启动异常** — 修复 `pipelineStarted` 消息在 `totalCount` 定义前读取变量的问题，避免 Pipeline 启动阶段直接失败
- **Pipeline warning 丢失** — 校验 warning 现在在 `pipelineStarted` 之后发送，Webview 能正确接收并在 `PipelineToolbar` 中展示
- **Pipeline 跳过状态不一致** — 新增 `pipelineNodeSkipped` 消息，跳过节点现在会正确更新进度统计和节点视觉状态，不再停留在 waiting
- **Pipeline 等待态误显示为运行中** — 启动 Pipeline 时改为清理旧的 `fn_status`，等待节点由 Pipeline 专属状态驱动，避免所有节点一开始都显示“运行中”
- **菜单入口范围偏窄** — Explorer “Add to Canvas” 与树菜单 “Open File” 现在覆盖数据、音视频、BibTeX、CSV/TSV 等已支持类型

---

## [2.0.0-alpha.4] — 2026-04-15

### Added
- **Pipeline 执行前校验** — `pipeline-validator.ts` 在 Pipeline 启动前检查所有节点：缺失输入连接、空 prompt 等问题自动检测，分为 error（阻止执行）和 warning（允许继续）两级
- **Pipeline 进度工具栏** — `PipelineToolbar` 浮动工具栏：进度条、暂停/恢复/取消按钮、当前执行节点名称、完成节点统计
- **Pipeline 状态机** — Zustand `PipelineState`：`nodeStatuses` / `completedNodes` / `isPaused` / `currentNodeId` / `validationWarnings` 完整状态管理
- **Pipeline 节点视觉状态** — FunctionNode 在 Pipeline 运行中显示不同状态：waiting 虚线边框、running 蓝色脉冲动画、done 绿色 ✅、failed 红色 ❌、skipped 灰色 ⊘
- **Pipeline 边动态着色** — `PipelineEdge` 根据执行状态动态变色：flowing 蓝色动画 → done 绿色 → failed 红色
- **BibTeX 文件支持** — code 节点新增 `.bib` / `.bst` 文件类型识别

### Changed
- `pipelineStarted` 消息携带完整元数据（节点列表、执行顺序、层级信息），UI 端一次初始化所有状态

---

## [2.0.0-alpha.3] — 2026-04-15

### Added
- **Pipeline 执行引擎** — `pipeline-engine.ts` 实现 BFS 下游节点发现 + Kahn 算法拓扑分层；`pipeline-runner.ts` 逐层执行、同层并行、中间结果通过 `injectedContents` 传递到下一层
- **Pipeline 运行按钮** — FunctionNode 新增 ▶▶ Pipeline 按钮，点击后从当前节点开始执行整条管道
- **Pipeline 消息链路** — `pipelineNodeStart` / `pipelineNodeComplete` / `pipelineNodeError` / `pipelineComplete` 消息类型，实时推送各节点执行进度

### Changed
- `function-runner.ts` 新增 `injectedContents` 参数，Pipeline 模式下跳过文件读取，直接使用上游节点的输出内容

---

## [2.0.0-alpha.2] — 2026-04-15

### Added
- **Pipeline 功能节点互连** — 功能节点之间现在可以用 `pipeline_flow` 边连接，构建多步骤 AI 处理管道
- **环路检测** — `graph-utils.ts` 新增 `wouldCreateCycle` 检测和 `topologicalSort` 拓扑排序，连线时实时阻止环路
- **Pipeline 边样式** — `PipelineEdge.tsx` 虚线紫色管道边，与普通 `data_flow` 边视觉区分
- **连接校验** — Canvas `isValidConnection` 实时校验连接合法性（类型检查 + 环路检测）

### Changed
- `EdgeType` 新增 `pipeline_flow` 类型
- `_createEdge` 重构为 `edgeType` 参数化，支持创建不同类型的边

---

## [2.0.0-alpha.1] — 2026-04-15

### Added
- **AI 内容理解度指示器** — `AiReadabilityBadge` 组件，在数据节点上显示 AI 对该内容的可读性评估：
  - PDF：显示可提取字符数 + 页数，检测到图表/公式时警告"含不可读内容"
  - Note / Code / AI Output：显示字符数，标注"完全可读"
  - Image：标注"需要多模态模型"
  - Audio / Video：标注转录需求
  - CSV：显示行列数
- **PDF 图表检测** — `content-extractor` 新增正则检测 PDF 中的 Figure / Table / Chart / Equation 引用，提示用户 AI 可能无法理解这些内容

### Changed
- `NodeMeta` 新增字段：`ai_readable_chars` / `pages` / `has_unreadable_content` / `unreadable_hint` / `csv_rows` / `csv_cols`

---

## [1.2.2] — 2026-04-14

### Fixed
- **画板拖动粘鼠板效应** — 拖动画板时不再"粘住"经过的节点。改为在拖动开始时快照内部节点，整个拖动过程中只移动快照中的节点，避免路过的节点被意外带走
- **AI 输出编辑后不更新** — 双击编辑 AI 输出节点后，节点卡片和预览窗口现在正确刷新。修复原因：`ai_output` 节点定义 `extensions: []`（无扩展名），导致文件变更监听器无法通过扩展名匹配到节点类型，现改为直接查询节点类型定义的 `watchContent` 标志
- **画布多节点时卡顿** — 移除 `onlyRenderVisibleElements`（ReactFlow 内置虚拟化）避免频繁挂载/卸载节点造成性能抖动；DataNode 和 FunctionNode 组件用 `React.memo()` 包裹，避免无关状态变更导致的不必要重渲染

---

## [1.2.1] — 2026-04-14

### Added
- **画布内置弹出式预览器** — 点击节点「预览」按钮，在画布内弹出全屏模态窗口预览内容，取代之前跳转 VSCode 编辑器的方式：
  - Markdown/笔记/AI 输出 → ReactMarkdown 全文渲染，14px 字体，完整 GFM 支持（表格、代码块、引用等）
  - 代码文件 → 等宽字体 + 行号 + 语言标签，全高度滚动
  - PDF → 多页渲染，所有页面上下滚动翻阅，页数统计
  - 图片 → 全尺寸居中显示（object-fit: contain）
  - 音频 → 波形可视化 + 播放/暂停控制 + 进度条（点击跳转）+ 时间显示
  - 视频 → 原生 `<video controls>` 全尺寸播放器
  - CSV/TSV → 全表格渲染，斑马条纹，列×行统计
  - 实验记录/任务清单 → 完整展示
  - ESC 键 / 点击背景关闭

### Changed
- **画板标题栏按钮放大** — ✎ 编辑按钮和 × 删除按钮字体大小与标题同步（24px / 28px），提升可点击性
- **画板标题自适应文字颜色** — 根据画板边框颜色亮度自动选择深色/浅色文字 + 对应阴影，确保任何颜色画板标题都清晰可读

---

## [1.2.0] — 2026-04-14

### Added
- **画板/工作区（Board）** — 全新的画布分区隔离系统，完全替代旧的「归纳组」功能：
  - 画板是半透明彩色矩形区域，节点放在上面，移动画板时内部节点跟随移动
  - 8 个控制点（四角+四边中点）自由调整画板大小
  - 工具栏「📋 画板」下拉：列出所有画板（点击跳转）、新建画板（名称+颜色选择）
  - 新建画板进入暂存架，拖到画布放置
  - 右键画板：编辑名称/颜色、删除画板
  - 动态归属：节点是否在画板内由 bounding box 重叠实时计算，无需手动管理
  - 旧画布的归纳组自动迁移为画板

### Removed
- **归纳组系统** — 被画板/工作区完全替代，选区浮动工具栏中的「归纳」按钮已移除

---

## [1.1.4] — 2026-04-14

### Fixed
- **归纳组输出归属** — 功能节点位于归纳组内时，AI 生成的输出节点自动加入同一归纳组，组边界自动扩展
- **归纳组标签可见性** — 标题栏高度 26→32px，字体 11→14px，加粗 600→700，增强多层文字阴影确保任何颜色下都清晰可读
- **生成历史功能移除** — 右键 AI 输出节点的"查看生成历史"菜单项已移除，避免画布崩溃
- **功能节点运行动效** — 运行中的功能节点显示流动渐变边框（蓝→紫→粉循环）+ 脉冲光晕效果，与暂存架风格一致

---

## [1.1.3] — 2026-04-14

### Fixed
- **fullContentCache 内存泄漏** — 节点删除后缓存条目未清理，在 `onNodesChange` 中新增自动清理逻辑
- **AudioContext 资源泄漏** — 波形生成出错时 `ctx.close()` 未调用，改用 `try/catch/finally`
- **PdfPreview 文档泄漏** — `pdfDoc.destroy()` 在错误和组件卸载路径未调用，现已补全
- **finishAiRun 重复节点** — AI 输出消息可能重复到达导致节点重复，新增 ID 去重检查

---

## [1.1.2] — 2026-04-14

### Added
- **PDF 节点第一页图形化展示** — Paper/PDF 节点从文字摘要改为渲染 PDF 第一页原图，使用 `pdfjs-dist`（v4.10.38）在 Webview 侧渲染第一页到 `<canvas>`
- **PdfPreview 组件** — blob worker 模式（兼容 IIFE 打包 + CSP），加载中/渲染失败状态处理，`renderedUri` ref 防重复渲染

### Changed
- Paper 节点不再走文字预览路径（`content_preview` 300 字符摘要保留用于搜索等场景）
- CSP 新增 `worker-src blob:` 允许 pdfjs blob worker

---

## [1.1.1] — 2026-04-14

### Added
- **画布按需渲染** — 启用 ReactFlow `onlyRenderVisibleElements`，视口外节点不挂载 DOM，大幅降低节点数量多时的内存和 CPU 占用
- **内容懒加载** — 节点进入视口时才请求完整内容（`requestFileContent`），离开视口后 DOM 卸载释放资源
- **fullContentCache 缓存** — 已加载的内容存入 store 级 Map，节点重新进入视口时直接命中缓存不重复请求，切换画布时自动清空

### Changed
- `content_preview`（300 字符摘要）仅作初始快速渲染用，全量内容不持久化到 `.rsws` 文件

---

## [1.1.0] — 2026-04-14

### Added
- **节点内容按需全量加载** — DataNode 挂载时通过 `requestFileContent` 向 Extension Host 请求完整文件内容，收到 `fileContent` 响应后自动替换为全量内容

### Changed
- `content_preview` 恢复为 300 字符摘要（仅用于初始快速渲染）
- 表格节点移除行列硬限制（原先 4 行 × 5 列），容器改为 `overflow:auto` 支持滚动
- `.rsws` 文件体积不受影响（全量内容不持久化，仅运行时加载）

---

## [1.0.9] — 2026-04-14

### Fixed
- **节点宽度爆炸** — `addNode`/`finishAiRun`/`commitStagingNode`/`createDataNode`/`createFunctionNode` 创建 FlowNode 时未设置 `width`/`height`，导致 ReactFlow 自动测量撑满容器；现已在所有创建点补全
- **内容不跟随填充** — 内容容器 `overflow:'hidden'` 改为 `overflow:'auto'` + `minHeight:0`；CodePreview 移除 5 行硬限制
- **updateNodeSize 一致性** — 同步更新 FlowNode 顶层 `width`/`height`

---

## [1.0.8] — 2026-04-14

### Fixed
- **节点初始大小异常** — `canvasToFlow` 新增尺寸合理性校验（width 120–800, height 50–1200），超出范围回退默认值
- **内容区域自适应** — 文本/代码/Markdown 预览移除固定 `maxHeight`，改用 `flex:1 + overflow:auto`；图片/视频预览改为百分比自适应

---

## [1.0.7] — 2026-04-14

### Changed
- **拖拽放入提示修正** — 恢复暂存架原始标题栏样式；新增拖拽文件进入画布时的全屏半透明蒙层（📥 图标 + 提示文字），拖拽离开或松手后自动消失

---

## [1.0.6] — 2026-04-14

### Added
- **代码节点渲染升级** — 深色背景、等宽字体、行号、语言标签（自动从文件扩展名推断）
- **节点可调整大小** — 所有数据节点新增 NodeResizer，选中后拖拽边角可自由缩放，尺寸自动持久化

### Changed
- 暂存架标题栏下方新增醒目横幅「↗ 拖至画布放置」

---

## [1.0.5] — 2026-04-14

### Added
- **Shift 拖拽提示** — 画布底部新增提示胶囊
- **暂存架流光动效** — 蓝→紫→粉渐变边框 + 呼吸光晕

### Fixed
- **宠物渲染尺寸跳变** — 不同动画 GIF 尺寸不一致导致宽度跳变，改用固定 bounding box + `object-fit:contain`

---

## [1.0.4] — 2026-04-14

### Fixed
- **宠物对话面板关闭后位置偏移** — 打开时保存原始位置，关闭时恢复

---

## [1.0.3] — 2026-04-14

### Fixed
- **多模态工具 activeRuns 泄漏** — TTS/STT/视频生成/图像编辑执行后 `activeRuns` 未清理（unreachable code）
- **新建画布同名文件夹冲突** — 若同名文件夹已存在但无 `.rsws` 文件，会尝试打开不存在的文件
- **syncDataNodeFile 空内容拦截** — `!content` 判断误拦截空字符串
- **_handleMessage 无顶层错误处理** — 任意 case 抛异常变为静默 unhandled rejection

---

## [1.0.2] — 2026-04-14

### Changed
- **节点紧凑布局** — 数据节点从固定 `height` 改为内容自适应（auto-size），消除底部空白

---

## [1.0.1] — 2026-04-14

### Fixed
- **实验/任务节点内容溢出** — DataNode 固定 `height` + 双重渲染导致溢出；改用 `minHeight` 自适应，排除专属 Body 组件的 text preview

---

## [1.0.0] — 2026-04-14

### Added
- **Per-Canvas 文件隔离** — 每个画布拥有独立工作空间文件夹（`{画布名}/{画布名}.rsws`），笔记、AI 输出、自定义工具、宠物状态均存储在画布子文件夹中，多画布完全隔离
- 新建画布自动创建同名文件夹；删除画布时清理整个文件夹

### Removed
- `aiOutputDir` 配置项（固定为 `outputs/`）

---

## [0.10.19] — 2026-04-14

### Fixed
- **实验/任务无法添加到画布** — 笔记/实验/任务标题输入从 Webview `window.prompt()`（被 VSCode iframe 阻止）改为 Extension Host 侧 `vscode.window.showInputBox()`

---

## [0.10.18] — 2026-04-13

### Changed
- **笔记/实验/任务统一存入 notes/ 文件夹** — 新建笔记改为保存到 `notes/` 子文件夹；实验记录和任务清单新增磁盘文件持久化（`.md` 格式）

---

## [0.10.17] — 2026-04-13

### Changed
- **Mermaid 图表提示词升级** — 系统提示词内置完整 Mermaid v11 语法参考（6 种图表 + 常见错误规避），输出改为标准 `.md` 文件

---

## [0.10.16] — 2026-04-13

### Fixed
- **宠物状态栏恢复** — PetChat 底部恢复显示心情/精力/经验完整信息
- **Mermaid 渲染错误处理** — 渲染失败时显示具体语法错误 + 原始代码 fallback，成功后新增「图表/代码」切换

### Added
- 空画布引导卡新增 ✕ 关闭按钮

---

## [0.10.15] — 2026-04-13

### Changed
- **Markdown 预览改为半窗口** — `markdown.showPreviewToSide`，不再全屏覆盖画布
- **宠物窗口越界修复** — 模式切换时自动 clamp 位置
- **PetChat 拖拽区域扩大** — 新增顶部标题栏作为拖拽区域

---

## [0.10.14] — 2026-04-13

### Changed
- **宠物自由放置** — 从四角吸附改为自由放置（拖到哪停在哪），位置用绝对 `left/top` 持久化
- **帮助弹窗内容更新** — 新增 CSV 表格节点、Shift+拖拽、归纳选色、宠物伴侣等说明

---

## [0.10.13] — 2026-04-13

### Changed
- **工具栏自适应布局** — 画布顶栏 `flexWrap` 自适应，窄窗口自动换行
- **自定义服务商图像支持** — 有图像时自动切换 OpenAI vision content array 格式

---

## [0.10.12] — 2026-04-13

### Fixed
- **CSV 导入修复** — StagingPanel 过滤器和 `isDataNode()` 缺少 `data` 类型

---

## [0.10.11] — 2026-04-13

### Added
- **CSV 表格节点** — `data` 节点类型（📊 图标），CSV/TSV 文件以表格样式展示（表头高亮、列数行数统计）

---

## [0.10.10] — 2026-04-13

### Fixed
- **宠物解锁修复** — 类型选择器改为按等级判断解锁
- **新增文件格式支持** — CSV/TSV/XML/HTML/CSS/SQL 等 15+ 种
- **Markdown 预览优化** — 仅打开渲染预览窗口

---

## [0.10.9] — 2026-04-13

### Changed
- **预览改用 VSCode 原生** — 移除 Webview 内置 PreviewModal，改为调用 `vscode.open`

---

## [0.10.8] — 2026-04-13

### Fixed
- **宠物对话无法发送** — vscodeApi 引用错误改用 `bridge.postMessage`

### Added
- 工作统计栏（本次时长/累计/连续天数）+ 📋 每日总结按钮

---

## [0.10.7] — 2026-04-13

### Added
- **宠物 AI 独立配置** — 支持独立服务商和模型（设置面板「宠物 AI」区域）
- **画布事件感知** — 添加/删除节点、AI 完成/出错时宠物即时反应

---

## [0.10.6] — 2026-04-13

### Changed
- **宠物浮动窗口打磨** — 气泡响应式宽度、点击展开对话、主题前景层独立裁切、入场淡入动画

---

## [0.10.5] — 2026-04-13

### Changed
- **宠物引擎改造** — 底部面板重构为浮动可拖拽窗口（最小化/漫游态/对话态），pointer-events 穿透不阻挡画布

---

## [0.10.4] — 2026-04-13

### Added
- **5 个场景主题背景** — 森林/城堡/秋天/海滩/冬天（来自 vscode-pets），背景/前景 PNG 分层渲染
- 设置面板新增场景主题选择器

---

## [0.10.3] — 2026-04-13

### Changed
- **宠物像素 GIF 素材** — 使用 vscode-pets（MIT）高质量像素 GIF 替换 Emoji 渲染，7 种宠物 × 6 个动画

---

## [0.10.2] — 2026-04-13

### Added
- **宠物 AI 感知 + 对话** — 感知画布内容，空闲 >5 分钟自动 AI 建议，双击打开对话弹窗，会话历史持久化

---

## [0.10.1] — 2026-04-13

### Added
- **宠物伴侣引擎 Phase 1** — 画布底部宠物面板，6 种像素宠物，Emoji 动画引擎，情绪系统，成长系统，休息提醒

---

## [0.10.0] — 2026-04-13

### Added
- **归纳颜色 + 编辑** — 创建时选择边框颜色（8 色预设）、创建后可编辑名称和颜色

### Fixed
- 自定义服务商输出节点标签显示 UUID 的问题

---

## [0.9.9] — 2026-04-13

### Changed
- **设置面板整理** — 按「LLM 文本服务商 / 多模态工具 / 其他」分组
- **工具栏统一** — `+ 图标 文字` 格式
- **拖拽导入** — 支持从 VSCode 资源管理器或系统文件管理器拖拽文件到画布

---

## [0.9.8] — 2026-04-13

### Added
- **AI 编排提示词** — AI 工具面板底部新增按钮，一键复制完整工具定义 JSON 规范提示词

### Changed
- 所有 LLM 工具系统 Prompt 编辑器显示默认提示词占位符
- 未连接输入时显示蓝色输入提示（按 apiType/slots 自动生成）

---

## [0.9.7] — 2026-04-13

### Fixed
- **工具导入防冲突** — ID/名称冲突自动追加后缀；磁盘文件名冲突检测

---

## [0.9.6] — 2026-04-13

### Added
- **撤销/重做** — Ctrl+Z / Cmd+Z 撤销、Ctrl+Shift+Z / Cmd+Shift+Z 重做；工具栏 ↩/↪ 按钮；覆盖节点/边/拖拽/连线/归纳等操作；最多 50 步

---

## [0.9.5] — 2026-04-13

### Changed
- **PDF 预览修复** — Paper 节点改为嵌入式 PDF 阅读器（`<embed>`）；CSP 添加 `frame-src`/`object-src`

---

## [0.9.4] — 2026-04-13

### Changed
- **预览按钮化** — 数据节点单击不再弹出预览，改为标题栏「预览」按钮触发

---

## [0.9.3] — 2026-04-13

### Fixed
- **归纳边界精确** — 使用 ReactFlow `getNodesBounds` 获取 DOM 实际测量尺寸；拖动节点同步更新归纳框；标题栏可拖拽整体移动

---

## [0.9.2] — 2026-04-13

### Changed
- **归纳简化** — 移除放缩功能，仅保留命名矩形框 + 删除；使用 ViewportPortal 渲染；画布缩放范围扩大至 5%–400%

---

## [0.9.1] — 2026-04-13

### Added
- **选区工具栏入口** — 工具栏「⬚ 选区」按钮切换框选模式

---

## [0.9.0] — 2026-04-13

### Added
- **选区工具 + 归纳** — 多选节点后浮动工具栏（移动/归纳）；归纳将选中节点包裹在命名矩形框中

---

## [0.8.8] — 2026-04-13

### Removed
- **移除分组和自动布局** — 删除 GroupNode 和 Dagre 自动布局功能（用户反馈会打乱布局）；清理 `@dagrejs/dagre` 依赖

---

## [0.8.7] — 2026-04-13

### Added
- **Auto-layout (Dagre)** — toolbar button "🔀 整理" automatically arranges all canvas nodes using Dagre hierarchical graph layout algorithm. Nodes are positioned left-to-right based on their connection relationships. Unconnected nodes are placed below. Group children are laid out internally.
- New dependency: `@dagrejs/dagre` for directed acyclic graph layout computation.

---

## [0.8.6] — 2026-04-13

### Added
- **Group nodes (folding groups)** — select multiple nodes on the canvas and click "📦 分组" in the toolbar to create a group. Group nodes act as visual containers with:
  - Dashed-border rectangle with colored accent
  - Title bar with rename (double-click), collapse/expand toggle
  - 8 color choices via right-click context menu
  - Collapse to single-line summary showing child count
  - Dissolve group (right-click → "🗑 解散分组") — children return to canvas
  - Resizable when selected (drag corners/edges)
  - Child nodes constrained within group bounds
- **`renameNode` store action** — inline rename for non-file nodes (groups, etc.)
- **`dissolveGroup` store action** — unparents all children and removes the group node

### Changed
- `NodeMeta` extended with `group_color` field.
- `FlowNode` / `FlowEdge` interfaces extended with `selected`, `hidden` fields.
- `canvasToFlow` now sets `style` dimensions for group nodes.
- Canvas nodeTypes registry includes `groupNode`.
- Toolbar has new "📦 分组" button.

---

## [0.8.5] — 2026-04-13

### Added
- **AI Tools panel grouped by category** — tools are now organized into 5 collapsible groups: Text Processing (summarize, polish, translate, review), Research (literature-review, outline-gen, rag), Multimodal (image-gen, image-edit, tts, stt, video-gen, image-to-video), Project Management (meeting-transcribe, action-items), General (chat, draw). Custom tools remain in their own section.
- **Tool search** — search input at the top of the AI Tools panel. Filters by name, description, and ID. Shows flat results while searching, grouped view when empty.
- **`category` field in JsonToolDef** — new optional field for tool categorization. All 17 built-in tool definitions updated.

### Fixed
- **Audio node stuck on "加载中"** — audio nodes never requested a webview URI (`requestImageUri` only handled `image` and `video`, not `audio`). Added `audio` to all 4 URI request locations. Also improved `useWaveform` hook with `<audio>` element fallback for duration and deterministic pseudo-random fallback waveform.

---

## [0.8.4] — 2026-04-13

### Added
- **Audio node waveform preview** — audio nodes now show a waveform visualization instead of a static icon. Waveform is generated from actual audio data using Web Audio API `decodeAudioData`.
- **Audio player modal** — single-click on audio node opens a modal with an interactive waveform player. Click on waveform to seek. Play/pause button with elapsed/total time display. Waveform bars highlight in blue as playback progresses.

### Removed
- **Run Guard selector** — removed "运行条件" dropdown (always/on-change/manual-confirm) from function nodes. Simplified to always run on button click.
- **Batch Run button** — removed "⚡ 批量" button from function node UI. The `runBatchFunction` message handler remains in the backend for future use.
- **Manual confirm dialog** — removed the confirm prompt that appeared when run_guard was set to manual-confirm.

### Changed
- Function node now has a single full-width "▶ 运行" button.
- Audio nodes are now included in `PREVIEWABLE` set for single-click preview.

---

## [0.8.3] — 2026-04-13

### Fixed
- **Critical: connections to no-slot tools silently dropped** — `onConnect` called `confirmConnection()` without first setting `pendingConnection`, so `confirmConnection` found a null connection and returned immediately. This caused **all** connections to tools without slots (TTS, image-gen, video-gen, image-edit, image-to-video, draw, chat, rag) to silently disappear after dragging. Refactored into `onConnect` → `_createEdge` for direct connections, `confirmConnection` only used after role picker dialog for slot-based tools.

---

## [0.8.2] — 2026-04-13

### Added
- **Experiment Log node (SR3)** — toolbar button `🧪 实验` creates an inline experiment log node. Fields: status (running/done/failed), date, params, result. All data stored in `meta`, serialized to `content_preview` for AI consumption. New `resources/nodes/experiment_log.json` definition.
- **Task node (PM1)** — toolbar button `✅ 任务` creates an inline task checklist node. Features: progress bar, checkbox toggle, add/remove items. Serialized as `任务进度: N/M (X%)\n[x] item\n[ ] item` for AI tools. New `resources/nodes/task.json` definition.

### Fixed
- **Staging note deletion** — removing a note node from the staging panel now also deletes the corresponding `.md` file on disk. Previously the file was orphaned.
- **TTS tool crash** — `runTts()` was missing `new AbortController()`, causing a runtime `controller is not defined` error. Fixed.
- **TTS/multimodal upstream check** — multimodal tools (TTS, image-gen, video-gen) no longer require upstream data nodes in the generic check. They have their own input validation.
- **TTS `text_input` param removed** — the redundant "文本（可选）" text input was removed from TTS. TTS now reads text exclusively from connected nodes (note, ai_output, etc.). FunctionNode shows warning when no upstream connected.
- **Multimodal model selector** — removed redundant "工具默认" / "全局默认" empty option from multimodal model dropdowns in both Settings panel and FunctionNode per-node selector. First model is now default.

### Changed
- `DataNodeType` extended with `experiment_log` and `task`.
- `NodeMeta` extended with experiment and task fields.
- `DEFAULT_SIZES` extended for new node types.
- `StagingPanel` and `FunctionNode` icon maps updated for `experiment_log` and `task`.
- `WebviewMessage` extended with `deleteNote` type.

---

## [0.8.0] — 2026-04-13

### Added
- **Batch Run (B1)** — each function node now shows a **⚡ 批量** button next to the Run button. Clicking it runs the tool once per upstream data node, producing a separate output node for each. Progress is shown as `批量运行 N/M…`. Runs sequentially to avoid API rate limits. New message type `runBatchFunction`; new `runBatchFunctionNode()` in `function-runner.ts`.
- **Output History (C3)** — right-click any `ai_output` node → **🕓 查看生成历史**: a modal lists all `.md` files in `.ai_outputs/`, newest first, with a 200-char preview per file. Click **恢复** on any entry to bring it back as a new staging node. Click **打开** to open it in the VSCode editor. Current node's file is marked with a "当前" badge.
- **Literature Review tool (SR1)** — new `literature-review.json`: batch-synthesizes multiple papers into paragraph synthesis, comparison table, or argument map. Slots: `论文` (multiple, required) + `综述方向说明` (optional reference note).
- **Outline Generation tool (SR2)** — new `outline-gen.json`: turns notes/drafts into a structured 2–4 level paper outline. Supports research paper / thesis / report styles. Slots: `笔记/草稿` (multiple, required) + `参考结构` (optional).
- **Meeting Transcription tool (PM2a)** — new `meeting-transcribe.json`: transcribes meeting audio via AIHubMix Whisper. Slot: `会议录音` (required audio node).
- **Action Items tool (PM2b)** — new `action-items.json`: extracts action items (who, what, when) from meeting transcripts or notes. Output as checklist or table. Slots: `会议记录` (required) + `上次行动项` (optional reference).

### Changed
- `OutputHistoryEntry` interface added to `canvas-model.ts`; `outputHistory` state + `setOutputHistory` action added to Zustand store.
- `requestOutputHistory` / `restoreOutputVersion` message types added to `WebviewMessage`.
- `outputHistory` added to `ExtensionMessage`.

---

## [0.7.4] — 2026-04-13

### Changed
- **Error experience (E2)** — error toasts now classify by type and show contextual action buttons:
  - **API Key 错误** (401 / unauthorized / "未配置…Key"): shows a **打开设置** button that opens the Settings panel directly.
  - **网络错误** (fetch failed / ECONNREFUSED / timeout): shows a hint to check network or Ollama service.
  - **文件缺失** (ENOENT / "找不到文件"): shows a hint to remove the node and re-import the file.
  - **其他错误**: auto-dismisses after 12 seconds.
  - All toasts still dismiss on click.

---

## [0.7.3] — 2026-04-13

### Added
- **Right-click menu unification (D2)** — consistent context menu across node types:
  - Data nodes: **打开文件** (open in VSCode editor), **重命名** (note nodes), **复制节点**, **从画布删除**
  - Function nodes: **重命名** (renames the node title in-place), **复制节点** (duplicates with all param values), **从画布删除**
  - New `duplicateNode` store action: clones a node 30px offset, resets fn_status to idle, saves canvas.
- **Empty canvas guide card (E1)** — when `nodes.length === 0`, a centered overlay card with three-step onboarding instructions and an "导入第一个文件" quick-action button is shown. Disappears automatically once the first node is added.

---

## [0.7.0] — 2026-04-13

### Changed
- **Removed blueprint engine**: deleted `blueprint-runner.ts`, `BlueprintNode.tsx`, `OrchestrationEditor.tsx` and all related state, message types, and UI (Blueprint tab in AI Tools panel, FunctionNode mini mode). `NodeType` no longer includes `blueprint`. The canvas now focuses on the simple, transparent data-node + function-node interaction model.

---

## [0.7.1] — 2026-04-13

### Added
- **Edge Role Labels (A0)** — when connecting a data node to a function node that defines `slots`, a role-picker dialog appears so the user can assign a semantic role to the connection. The role label (e.g. `原文`, `术语表`) is displayed as a pill badge at the edge midpoint.
  - `CanvasEdge` gained an optional `role` field (fully backwards-compatible — existing `.rsws` files are unaffected).
  - `JsonToolDef` gained an optional `slots: SlotDef[]` field.
  - `Shift`-drag or tools without `slots` skip the dialog and create a generic edge immediately.
  - `function-runner.ts`: upstream content blocks are now grouped by role with `## <role label>` headers before being sent to the AI, so the model can clearly distinguish "原文" from "参考建议", "待翻译文本" from "术语表", etc.
  - Built-in tools updated: **polish**, **translate**, and **review** now ship with `slots` definitions.

---

## [0.7.2] — 2026-04-13

### Added
- **Output placement (A1)** — `calcOutputPosition()` uses axis-aligned bounding-box collision detection to place new AI output nodes in the first non-overlapping slot to the right of the function node (up to 20 candidates, then appends below the lowest existing node). Eliminates piled-up output nodes after repeated runs.
- **Streaming progress feedback (F1)** — while a function node is running, a throttled (500 ms) progress update pushes character count + an 80-character inline peek preview to the webview. Displayed as a tinted preview box below the function node status line.
- **Run guard (F2)** — each function node can be configured with a run condition:
  - `always` (default) — runs unconditionally on every click.
  - `on-change` — computes a djb2 fingerprint of all upstream content; skips the AI call and shows "已是最新（输入未变化）" if nothing changed since last run.
  - `manual-confirm` — shows an inline confirm dialog before executing; useful for expensive or destructive operations.
  - Run guard is stored in `node.meta.run_guard` (not `param_values`) to keep AI parameters separate from node behavior.
- **Prompt parameterization (F3)** — `summarize` tool gains a free-text **关注点** (`focus`) parameter and a `slots` definition. The system prompt template uses `{{focus}}` substitution (already supported by `ToolRegistry.buildSystem()`) so the focus phrase is injected directly into the prompt without extra user effort.

---

## [0.6.15] — 2026-04-12

### Added
- **Node right-click context menu** — all node types (data, function, blueprint) support right-click delete
- **Note rename** — right-click a note node to rename it inline; renames the underlying file via `WorkspaceEdit.renameFile` (VSCode-tracked), detects name conflicts and suggests alternatives, updates canvas immediately without relying on `onDidRenameFiles`

### Fixed
- `window.prompt()` is blocked in VSCode webview — replaced with inline React input UI inside the context menu portal
- After rename, node showed as "missing" with old title — fixed by directly updating canvas data and pushing `nodeFileMoved` message instead of relying on file-system events

## [0.6.14] — 2026-04-12

### Fixed
- **Ollama multimodal false warning** — removed the `VISION_KEYWORDS` heuristic that incorrectly flagged models like `qwen3.5` as "may not support images". Image handling is now unconditional; the model itself handles unsupported inputs

## [0.5.2] — 2026-04-12

### Fixed
- **ResizeObserver fatal error** — `window.error` handler was treating the benign `ResizeObserver loop completed with undelivered notifications` browser notification as a crash, replacing the entire UI with a fatal error screen. Now filtered out; ReactFlow/MiniMap resize is no longer fatal

### Changed
- **BlueprintNode visual redesign** — complete style overhaul:
  - Canvas card (240 px wide): header with title + `SERIAL`/`PARALLEL` pill; step preview rows with color-coded left border, number badge, tool name, status dot; footer with status indicator + step count + **详细** + **▶ Run**; drop-shadow + selection ring
  - Detail modal: blurred backdrop; large header with `StatusPill` + `ModePill` + error-strategy badge; toolbar with add-step dropdown, mode/error toggles, save-def, run; step cards with param-chip previews; inline `StepParamForm` slides in above list on ⚙; clean connector arrows between serial steps



### Fixed
- **BlueprintNode layout** — blueprint child function nodes were incorrectly rendered as independent ReactFlow nodes on the canvas, floating on top of the blueprint card and obscuring its step preview. Fix: `canvasToFlow` now excludes all nodes whose `parent_group_id` points to a blueprint node from the ReactFlow node list; their info is read directly by `getBlueprintChildNodes` and displayed inside the compact card and detail modal
- **Blueprint card redesign** — replaced the old fixed-size scrollable box with a compact self-sizing card (max 3 steps preview + "…N more") and a full detail modal (portal to `document.body`, ESC/backdrop-click to close) containing the full step list, inline param forms, toolbar for add/reorder/delete/exec-mode/on-error/save-def/run

## [0.5.0] — 2026-04-12

### Added
- **Multimodal tools** (AIHubMix API Key required):
  - **Image Generation** — FLUX.1-Kontext-pro / DALL-E / Ideogram text-to-image
  - **Image Editing** — FLUX Kontext / GPT-4o edit with reference image
  - **Text-to-Speech** — tts-1 / tts-1-hd / Gemini TTS; outputs audio node
  - **Speech-to-Text** — Whisper transcription/translation; outputs ai_output node
  - **Video Generation** — Wan / Kling / HailuoAI text-to-video; outputs video node
  - **Image-to-Video** — Wan / Kling image-to-video with motion prompt
- **`audio` / `video` node types** — new data node types with 🎵 / 🎬 card UI
- **AIHubMix API Key** setting in Settings panel and `researchSpace.ai.aiHubMixApiKey` config
- **Blueprints tab** in AI Tools panel — browse, preview steps, drag-to-instantiate, import/export/delete
- **BlueprintNode edit mode** — double-click for blue-border edit mode with toolbar: add step, move up/down, remove, configure params, toggle serial/parallel, toggle on_error, save definition

### Fixed
- `finishAiRun` now calls `debouncedSave` so multimodal output nodes persist across canvas close/reopen

## [0.4.0] — 2026-04-12

### Added
- **Blueprint engine rewrite**: `JsonBlueprintDef` JSON-driven blueprint definitions
- **BlueprintRegistry** — 3 built-in blueprints (`summarize-polish`, `rag-report`, `review-translate`) + user `.rsblueprints/` hot-reload
- **Kahn topological sort** — guarantees correct step execution order via internal `data_flow` edges
- **Serial chain data passing** — previous step output injected into next step without disk I/O
- `on_error` strategy (`stop` / `continue`) per blueprint node
- `instantiateBlueprint` — creates container node + child nodes + internal edges from a `JsonBlueprintDef`
- 11 blueprint orchestration message types (`addBlueprintStep`, `removeBlueprintStep`, `moveBlueprintStep`, `addBlueprintChannel`, `updateBlueprintChannel`, `removeBlueprintChannel`, `setBlueprintExecMode`, `setBlueprintOnError`, `updateBlueprintStepParams`, `saveBlueprintDef`)
- Blueprint import / export / delete
- `.rsblueprints/` file watcher for hot-reload
- `canvas-store`: `blueprintDefs`, `editingBlueprintId`, `getBlueprintChildNodes`
- `function-runner`: `FunctionRunResult` return type (`success`, `outputContent`, `outputNode`)
- `content-extractor`: `injectedContents` parameter for blueprint serial injection

## [0.3.1] — 2026-04-12

### Changed
- Removed "Restore Viewport" button; MiniMap is now pannable + zoomable for quick navigation

### Added
- **Input ordering** — drag ⠿ handles inside function nodes to reorder connected files; order persists to `input_order` and affects AI processing sequence
- **Chat tool** — custom prompt textarea with file pill tags; click a tag to insert `@filename` reference; `function-runner` resolves `@ref` to file content at run time

## [0.3.0] — 2026-04-12

### Added
- Double-click node opens file in `ViewColumn.Beside` so canvas stays visible
- Viewport persistence — pan/zoom written to `.rsws`; toolbar "Restore / Fit" toggle
- `DataNodeRegistry` — node metadata (icon, color, extensions, preview type) driven by `resources/nodes/*.json`; eliminates 13 hardcoded mappings

## [0.2.9] — 2026-04-12

### Added
- AI Tools panel groups builtins vs. custom tools
- Tool import / export / delete
- `.rstools/` hot-reload watcher — custom tools reload without reopening canvas
- `validateToolDef` error toast on malformed JSON

## [0.2.8] — 2026-04-12

### Added
- Staging panel persistence — `stagingNodes` written to `.rsws`; restored on reopen
- JSON-driven tool engine — `ToolRegistry` loads `resources/tools/*.json`; `AiToolsPanel` renders dynamically
- User `.rstools/` directory for custom tool definitions

## [0.2.0–0.2.7] — 2026-04-11 / 12

### Added
- Embedded Settings panel (⚙) with per-provider config (API Key, Model, Base URL)
- Custom OpenAI-compatible providers (e.g. AIHubMix)
- Copilot global default model; model list auto-fetched from API
- Node drag fix: skip `debouncedSave` while dragging
- Stability fixes: dirty-dot, listener leaks, `suppressNextRevert`, `Promise.allSettled`
- Canvas crash fix: `writeCanvas` uses direct overwrite instead of tmp+rename
- Double-click nodes open in VSCode built-in viewer
- Markdown rendering preview in `note` / `ai_output` node cards

## [0.1.0–0.1.9] — 2026-04-11

- Initial release: Custom Editor, ReactFlow canvas, data nodes, function nodes, three-tier AI provider fallback (Copilot → Anthropic → Ollama)
- Per-node Provider / Model selector; System Prompt editor
- RAG query input; Draw (Mermaid) tool
- Sidebar tree view with right-click context menus
- Staging Panel — new nodes land in floating tray before placement
- File sync: rename tracking, content_preview refresh, delete watcher
